---
name: AWS Terraform future work
description: User plans to tackle the aws-terraform directory in a future session
type: project
---

AWS Terraform work (`aws-terraform/`) is planned for a future session.

**Why:** User flagged this as upcoming work after completing the ansible improvements.

**How to apply:** When the user starts a new session mentioning AWS, Terraform, or infrastructure, proactively check the `aws-terraform/` directory and bring relevant context from the ansible deep dive (environments, conventions, vault patterns already established).

#!/usr/bin/env bash
printf "claude %s\n" "$*" >> "${MOCK_CALLS_FILE:-/tmp/mock_calls}"
case "$1-$2" in
  plugins-list)
    printf "%s\n" "${MOCK_CLAUDE_PLUGINS_LIST_OUTPUT:-}"
    exit 0
    ;;
  *)
    exit "${MOCK_CLAUDE_EXIT:-0}"
    ;;
esac

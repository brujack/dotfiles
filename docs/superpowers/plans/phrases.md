# FROZEN at e6167816. These anchors describe CLAUDE.md as #293's reviewers judged it.
# The 2026-09-25 relocation moved many of them to ai-config/docs/knowledge/dotfiles-*.md
# and they are deliberately not updated. Any change that wires phrase_check.py into a
# gate must re-anchor first.

# Phrase manifest — CLAUDE.md four-class re-sort
#
# class | phrase | counterpart-file | counterpart-symbol | note
#
# Fragments: A (Layout/Symlink/Key Conventions), B (Testing block),
# C (Test Seams, MAKEFLAGS, remaining sections), D (gap-fill).
# Two content paragraphs are documented exemptions, proven unsatisfiable:
#   `@~/.claude/standards/powershell.md`  and  `CI requirements:`

# Phrase manifest — fragment A (Layout, Symlink Strategy incl. Cursor Parity, Key Conventions)
#
# class | phrase | counterpart-file | counterpart-symbol | note

# ---- Layout ----
HAZARD | config, teleport.cfg only — never private keys | - | - | .ssh/ tree comment: a session could add a private key file here and nothing in the repo structure would stop it; the consequence (never commit private keys) is not derivable from reading the tree alone.
RECORD-deleted | rather than by memory; it has drifted twice by silent omission | - | Coverage Denominators section | Deleted: the parenthetical (drift count + correction date) was pure provenance for an already-self-sufficient rule ("Regenerate with `git ls-files ...` rather than by memory."); removed from CLAUDE.md. Counterpart field cleared: it named ~/.claude/standards/tdd.md as a see-also for the omission-cannot-lower-a-ratio framing, never as a phrase-level claim, and this row's own note said so. Measured: the phrase occurs 0 times there, as do 'drifted twice' and 'silent omission'. A RECORD deletion is justified by git history, not a counterpart.

# ---- Symlink Strategy ----
REFERENCE | creates symlinks from `$HOME` into the repos | - | - | Plain orientation sentence, discoverable by inspecting $HOME and the repos directly.
REFERENCE | individually into `$HOME` (e.g. `~/.zshrc → dotfiles/.zshrc`) | - | - | Mechanical description of the repo-root symlink convention, visible from `ls -la ~`.
HAZARD | except `rules/`, which is never linked, and `projects/`, which the loop skips and then links explicitly | - | - | Names two exceptions to the naive "every item is linked" assumption; a session that assumed uniform linking would misjudge what `~/.claude/rules` or `~/.claude/projects` actually are.
AMBIGUOUS | reports `[PASS] ~/.claude/mcp.json (generated)` (`lib/helpers.sh:757`) for a path it only tested for existence | - | - | This blank-line block bundles three distinct claims: a dated correction about mcp.json being a symlink (RECORD, "until 2026-09-14"), a standing operational hazard that `-t doctor` cannot verify the guard (HAZARD), and the rationale for never linking rules/ (REFERENCE). No single class covers the whole block honestly.
DUPLICATE-partial | until 2026-08-25. | ~/.claude/CLAUDE.md | Machine Setup section, "`projects/` is NOT excluded" paragraph | Same argument (a stale claim that projects/ was excluded, corrected on the same date) in both files. Verified: `until 2026-08-25.` occurs in both dotfiles/CLAUDE.md and ~/.claude/CLAUDE.md. VERDICT: partial — only the fragment "until 2026-08-25." is a literal duplicate (phrase_check confirms it occurs once in both files); every full sentence in the surrounding block is dotfiles-specific — a readlink-verified code walkthrough here vs ai-config's inode verification there, and a distinct incident date (2026-08-25, three drafts/two sessions) vs ai-config's own (2026-08-21 and 2026-08-24). 0 of 9 candidate sentences match verbatim. Nothing removed; the shared date is coincidental provenance (both files' own text was corrected the same day), not duplicated content.
DUPLICATE-withdrawn | is inside ai-config's `make test` scope, where `validate-memory` is a `test` prerequisite | ~/.claude/CLAUDE.md | Machine Setup section, "This matters most to sessions that are NOT working in ai-config" paragraph | Same argument (writes under ~/.claude/projects/ land inside ai-config's make test scope and block commits) in both files. Verified via shared exact substring `make test` scope present in both. VERDICT: withdrawn — phrase_check.match_phrase finds 0 occurrences of the full declared phrase in the counterpart (~/.claude/CLAUDE.md); only the 4-word substring "make test` scope" is genuinely shared, and the surrounding sentences differ (`validate-memory` vs `validate_memory.py`, and different measured dates/session counts). Cannot satisfy --deleted-have-counterparts. Nothing removed.
REFERENCE | each item (excluding `User/`) symlinked individually into `~/.cursor/` from the ai-config repo | - | - | Mechanical description of the .cursor/ symlink convention.
REFERENCE | remove the old file before symlinking (`rm -f` then `ln -s`) | - | - | Plain mechanical convention for symlink creation, no hidden consequence.
REFERENCE | share the same plugin/skills files on disk — but Cursor rules, model settings, and MCP server registration live in `.cursor/User/` | - | - | Describes the Cursor/Claude Code parity mechanism; discoverable by inspecting the symlinked directories.

# ---- Key Conventions ----
REFERENCE | driven by the **profile/capability model** in `config/profiles.sh` — prefer `HAS_*` vars over raw hostname patterns for new code | - | - | States the current convention; the model itself is visible in config/profiles.sh.
HAZARD | would fire on any future GPU-less machine with that profile, and on WSL2 where the driver lives Windows-side | - | - | Explains why GPU provisioning deliberately does not use a HAS_* capability; a session following the HAS_* convention blindly would misapply it here.
HAZARD | nouveau holds the card until a reboot, so `_install_ubuntu_nvidia` warns rather than implying the GPU is live | - | - | A correctly-provisioned box can still be running the wrong driver until reboot — not evident from reading the install code alone.
HAZARD | fails with `AMD CDI spec not found`: an AMD-named error for a missing **NVIDIA** runtime, which misdirects whoever reads it next | - | - | The error message actively misdirects diagnosis toward the wrong vendor; a session debugging this without the note would chase the wrong subsystem.
REFERENCE | `WORKSTATION` and `CRUNCHER` remain live, and are read by `.zprofile:10` and `.config/.zshrc.d/7_final.zsh:60` | - | - | Factual pointer to current live legacy variable consumers.
REFERENCE | Ubuntu version detection uses `lsb_release -rs` → `NOBLE` var (24.04) or `RESOLUTE` var (26.04) | - | - | Plain factual convention, visible in detect_env.sh.
REFERENCE | Credential directories (`.aws`, `.tf_creds`, `.tsh`) are created with `chmod 700` | - | - | Plain factual convention.
REFERENCE | Git repos are cloned to `~/git-repos/personal/` and `~/git-repos/work/` | - | - | Plain factual convention.
REFERENCE | Python environments managed via **pyenv** + **pyenv-virtualenv**; the `ansible` venv is the primary one | - | - | Plain factual convention.
REFERENCE | is gated to macOS by a `sys_platform == 'darwin'` marker in the manifest, not by a shell branch | - | - | Describes current mechanism, visible in pyproject.toml.
HAZARD | run `brew uninstall ruff` once after venv recreate to remove the legacy brew install | - | - | A one-time remediation step; skipping it leaves a stale brew-managed ruff that could shadow the venv-managed one, a consequence not obvious from the venv recreation code itself.
REFERENCE | `pytest` — runs `unittest.TestCase` tests natively; test file contents do not change | - | - | Plain factual convention about the test runner.
REFERENCE | Application installs are kept in alphabetical order | - | - | Plain style convention.
DUPLICATE-judgement | for any file `scripts/list-shell-files.sh` picks up (every tracked bash/sh-shebang file, extension or not | CLAUDE.md | Testing section, "make lint" bullet (`make lint`'s scope comes from `scripts/list-shell-files.sh`) | CLAUDE.md (Testing section, `make lint` bullet, ~line 272): "`make lint` — `bash -n` over `SHELL_FILES` (derived by `scripts/list-shell-files.sh`, which emits every tracked file whose first line is a bash/sh shebang — 107 files, measured 2026-09-08, including the `tests/mocks/` fixtures and the two extensionless hooks)". CLAUDE.md (Key Conventions, shell-syntax-fix bullet, ~line 1039): "validate with `bash -n <file>` for any file `scripts/list-shell-files.sh` picks up (every tracked bash/sh-shebang file, extension or not — includes the hooks and the `tests/mocks/` fixtures)". Measured: phrase_check.match_phrase(normalize(CLAUDE.md), the full declared phrase) = 1 — the declared phrase itself lives only at the Key Conventions occurrence; the counterpart-file field for this row is CLAUDE.md itself, so this is the one row of the ten this task set out to disposition whose counterpart is an internal duplicate, not an external one. The manifest format cannot express this case: marking the row deleted would make --deleted-have-counterparts re-open CLAUDE.md — the file this row's own deletion would just have edited — search it for the declared phrase, and find 0 occurrences (the sole instance is the one being deleted), failing the gate on a row that is, in substance, a genuine duplicate. Shared: both occurrences state the identical fact (list-shell-files.sh's shebang-derived scope reaches every tracked bash/sh file including extensionless hooks and `tests/mocks/` fixtures). Diverges: the Testing-section occurrence is the fuller explanation — it names the file count (107, measured 2026-09-08) and the derivation mechanism (`SHELL_FILES`, shebang-first-line test); the Key Conventions occurrence is a terser usage tip attached to "how do I validate a syntax fix", without the count or the mechanism, just the consequence (which files are covered). VERDICT: judgement — reclassified from a prior withdrawn disposition to this task's format; the withdrawn note's substance (kept Testing's fuller explanation as canonical, left the Key Conventions usage tip as-is) still holds. Nothing removed.
RECORD-deleted | the worktree's `1_init.zsh` carried a new `source` line, the main checkout carried zero occurrences of it, and `zsh -i -c exit` returned 0 throughout | - | - | Deleted: the standing hazard ("In a worktree that command is evidence about the main checkout, not about your branch") and the remedy that follows both stand without this dated measurement; removed from CLAUDE.md.
DUPLICATE-withdrawn | lost `PROFILE` and every `HAS_*`, and then skipped the `/opt/homebrew/bin` prepend that `pyenv init` depends on | ~/.claude/standards/shell.md | zsh section, "`$0` is not the file's own path in a zsh startup file" entry | Same code defect and same measured failure (`${0:A:h}` resolving against cwd, losing PROFILE/HAS_*, skipping the pyenv prepend) as shell.md's worked example, which itself names config/profiles.zsh. Verified via shared exact substring "for the source builtin and for functions" present in both. VERDICT: withdrawn — shell.md's counterpart reads `pyenv init --path` where the declared phrase reads `pyenv init`, so phrase_check.match_phrase finds 0 occurrences. The paragraph substantially paraphrases shell.md's zsh entry (which already names this repo's own config/profiles.zsh as its worked example) but no complete sentence is a literal duplicate (0 of 8 candidates match verbatim). Cannot satisfy --deleted-have-counterparts. Nothing removed.
RECORD-withdrawn | `_update_summary` `continue`s past any array entry with no `status_<name>` file (`lib/update_summary.sh:547`) | - | - | Withdrawn: this is the mechanism itself, not bare provenance — it is the only place CLAUDE.md explains WHY the retired count-assertion warning was wrong, and the next sentence ("because an unseeded array entry is invisible to the tally") depends on it. Deleting it would strand "is stale and was measured wrong" with no reason given. Nothing removed.
HAZARD | it performs real `git push`/`rsync --delete` over SSH against real hosts, and `_is_legacy_sync_host` triggers on the real `hostname -s` | - | - | Running this script unmocked outside the test harness has real destructive consequences against real hosts — not evident from the script's name alone.
HAZARD | the `run_update` mapping the section renders `[OK] git-hooks updated` over its own findings | - | - | A silent-success failure mode: a missing rc mapping makes a broken hook sweep report success, indistinguishable from a real pass by output alone.
RECORD-deleted | which is how `brew_install_formula go-task/tap/go-task` failed with **exit 127 on every Linux run since it was added** and still reported success | - | - | Deleted as written, then partly restored in compressed form after pr-review cycle 2 measured that removing it orphaned two referents: "not hypothetical" lost its evidence and "The tap-qualified name" lost its antecedent. CLAUDE.md now carries "so `go-task/tap/go-task` exited 127 on every Linux run while reporting success" -- the incident, without the since-it-was-added provenance. This note's first version claimed the causal explanation already carried the reason; that was false and is the finding.
RECORD-withdrawn | Two earlier versions of this line were wrong and are retracted | - | - | Withdrawn: names two specific prior misreadings ("the singular verb", unscoped-registers-at-project-scope) that a returning reader could otherwise reintroduce; this is exactly the reversal-narrative this file deliberately keeps, not a bare dated figure (no date is even present). Nothing removed.
HAZARD | tightening to `-d` would route that layout to `SKIP` forever | - | - | A plausible "cleanup" edit (`-e` to `-d`) would silently disable updates for submodule/worktree layouts — not evident without the mutation-testing note.
HAZARD | a future section name of 20 characters or more fails this test rather than silently colliding with the reason column | - | - | Names a coupling a future editor would not otherwise know about: adding a longer `_UPDATE_SECTION_ORDER` name has a hard-coded consequence in a test.
HAZARD | a banner line in that budget would silently displace real diagnostic content | - | - | Explains why progress banners must print outside the subshell; violating this silently corrupts the tail -10 diagnostic budget.
HAZARD | makes the `execution_profiles` `command_denylist` inert whenever auto-approve is on | - | - | A Warp default that silently defeats a security control; not evident from reading settings.toml alone.
HAZARD | **empty or whitespace-only** value is a real pin, not an absent one | - | - | Counter-intuitive: an empty core.hooksPath value still disables every hook on the machine, which a naive truthiness check would miss.
HAZARD | byte-identical to a genuinely unset key — while `rev-parse --git-path hooks` returned the pinned path and every repo on the box was silently redirected | - | - | A `git config --get` check without `--includes` cannot distinguish a real pin from an unset key, producing a false-clean report while every repo is actually redirected.
HAZARD | an append here would be completely inert and would still look correct to a reader | - | - | The PATH prepend must be a prepend, not append-via-`+=`; the append form silently fails to change resolution while looking like ordinary code.
RECORD-withdrawn | The full suite was run under both versions sequentially: `rc=0, 1363 ok, 0 not ok` under each, with an empty `not ok` diff | - | - | Withdrawn: "Do not reopen this without re-running that comparison" a sentence later refers back to exactly this measurement ("that comparison"); deleting it orphans the reference. Nothing removed.
HAZARD | production code prepending a real directory from inside the hook wins that race and the hook then runs the real suite against a bats fixture | - | - | A PATH manipulation inside a hook can silently shadow the test harness's own mock, producing a hook that tests the wrong thing without any error.
RECORD-partial | advice that is wrong, because bootstrap already ran | - | - | Partial: the printed error message and the fact that its own advice is wrong are load-bearing (a reader debugging this would otherwise follow the bad "re-run bootstrap" advice) and stay; only the rotting duration figure ("nineteen months ago") was deleted from CLAUDE.md, since it decays with every month that passes and is not needed to know the advice is wrong. Re-anchored: the deleted duration figure took the original phrase with it; re-pointed at the surviving clause in the same paragraph (CLAUDE.md, count 1, not sentence-initial).
HAZARD | judges each `CARGO_TOOLS` pin (`lib/constants.sh`) by whether the binary runs (`--help`), never by the version string cargo reports | - | - | A session assuming version-string comparison would misunderstand why a linked-but-broken binary gets reinstalled; the repair criterion is not what a reader would guess.
REFERENCE | installs `TERRAFORM_VER` only when `~/.tfenv/version` does not exist yet — an operator's chosen version (`workstation` stays on 1.14.9) is never overwritten | - | - | Describes current idempotent install behavior, visible in the function itself.
HAZARD | run the loop first and it plants two dangling symlinks, after which its own `-L` branch sees `readlink` already equal to the target, `continue`s, and repairs nothing on every later run | - | - | Ordering is load-bearing in a way that is easy to "fix" backwards: reordering the guard and the symlink loop silently defeats the repair permanently.
HAZARD | needs `-A "dotfiles check-versions (bjackson@pobox.com)"` or the request 403s (measured from all three development machines on 2026-09-17) | - | - | crates.io silently 403s without a User-Agent header; a session adding a new cargo-tool version check without this header would get an opaque failure.
# phrases-b.md -- CLAUDE.md fragment B: Testing intro, ShellCheck, Testing Rules,
# PowerShell Testing, Coverage, Mock Pattern (Test Seams and MAKEFLAGS excluded --
# owned by fragment C / Task 5). See docs/superpowers/plans/2026-09-21-claude-md-four-class-resort.md Task 4.
HAZARD | (Bash Automated Testing System), installed natively: | - | - | -
HAZARD | is a platform dispatcher (mirrors `install_zsh()`'s shape): `run_setup_user` calls it unconditionally on both macOS and Linux, so bats provisioning is no longer Linux-only. | - | - | -
HAZARD | `install_bats_macos()` in `lib/macos.sh` — `brew install bats-core` (also listed in `Brewfile` for `brew bundle` parity) | - | - | -
HAZARD | `install_bats_linux()` in `lib/linux_shared.sh` — `sudo apt-get install -y bats` | - | - | -
HAZARD | `make test` (runs lint, the lock and requirements-CI drift checks, the Python suite, then all BATS tests) | - | - | -
HAZARD | runs bats at `--jobs $(JOBS)` when **GNU** parallel is present, and serially with a notice when it is not. Three things about that are load-bearing: | - | - | -
DUPLICATE-judgement | ships an incompatible `parallel` under the same name, so `command -v parallel` is true for it; `HAVE_PARALLEL` greps `parallel --version` for `^GNU parallel` instead. | ~/.claude/standards/shell.md | moreutils `parallel` detection (`"command -v parallel" is not a GNU-parallel check...`) | CLAUDE.md (Testing section, parallel-detection bullet): "moreutils ships an incompatible `parallel` under the same name, so `command -v parallel` is true for it; `HAVE_PARALLEL` greps `parallel --version` for `^GNU parallel` instead." shell.md ("`command -v parallel` is not a GNU-parallel check, and `bats --jobs` needs the GNU one"): "moreutils ships its own incompatible `parallel` under the same name." plus the Makefile line `HAVE_PARALLEL := $(shell parallel --version 2>/dev/null | grep -q '^GNU parallel' && echo yes)`. Measured: phrase_check.match_phrase(normalize(shell.md), the full declared phrase) = 0 — the wording never lines up verbatim ("ships an incompatible" vs "ships its own incompatible", and CLAUDE.md states as prose what shell.md states as a code diff). Shared: identical detection rule (ask `parallel --version`, don't trust `command -v`). Diverges: shell.md carries the dpkg-diversion/Homebrew-namespacing mechanics and the Wrong/Correct Makefile block; CLAUDE.md's copy adds this repo's own consequence — `bats-exec-suite:106`'s inverted guard, which shell.md never mentions. VERDICT: judgement — same argument, different prose; --deleted-have-counterparts cannot authorise removal. Left for the compression plan to merge both copies. Nothing removed.
HAZARD | defaults to 24 and is validated in pure make at parse time** — no `$(shell)`, no fork. Override it per invocation (`make test JOBS=6`). | - | - | -
HAZARD | is a filesystem walk, not `git ls-files`.** An untracked `.bats` file is exactly what a TDD red step produces, and a tracked-only list would report it green by never running it. | - | - | -
HAZARD | carves a file out of the parallel pool to run alone afterwards. It is empty: the per-file check measured every file clean at `--jobs 12`, and the one that was not was fixed rather than exempted. | - | - | -
HAZARD | preinstalls GNU parallel, so `HAVE_PARALLEL` resolves to `yes` on the runner whatever the workflow installs — a claim to the contrary read off `ci.yml` is reading a file with no field for what the image ships (ai-config ADR-0078 Amendment 2). | - | - | -
DUPLICATE-judgement | on a 2-vCPU runner that is two workers, which ai-config's runner data shows is the worst available setting — slower than serial, while a fixed count well above the core count beats it. | ~/.claude/standards/ci.md | `$(nproc)` is the worst worker count on a 2-vCPU runner (measured in `ADR-0078 Amendment 2`) | CLAUDE.md (Testing section, "CI takes the parallel path too" paragraph): "on a 2-vCPU runner that is two workers, which ai-config's runner data shows is the worst available setting — slower than serial, while a fixed count well above the core count beats it." ci.md (Amendment 2, the `$(nproc)` measurement): "a private repo's runner has 2 vCPUs, so `$(nproc)` is the worst available worker count, not a sensible default. A fixed count well above the core count beat serial; matching the core count lost to it." Measured: phrase_check.match_phrase(normalize(ci.md), the full declared phrase) = 0 — CLAUDE.md's summary sentence never appears as ci.md's own words ("slower than serial" vs "beat serial"; present tense throughout CLAUDE.md vs past tense throughout ci.md). Shared: identical conclusion (nproc=2 is the worst setting on a 2-vCPU runner; a fixed count above the core count wins). Diverges: ci.md carries the measured arm table (serial 324s n=15, nproc=2 374s n=1, jobs 12 279s n=2) that produced the conclusion; CLAUDE.md states the conclusion as a directive ("Never pass `JOBS=$(nproc)`") and points at this repo's own ADR-0035 for its own figures. VERDICT: judgement — same argument, different prose. Nothing removed.
HAZARD | (runs `unit.bats`, `profiles.bats`, and `zshrc.d/unit.bats`) | - | - | -
HAZARD | a `brew install` on Linux would put an unmanaged linuxbrew copy ahead of the pinned `/usr/local/bin/shellcheck`. The recipe reads `_OVERRIDE_PLATFORM` | - | - | - Re-anchored by the post-merge docs audit: the previous span carried the '107 files, measured 2026-09-08' figure, which had rotted to 110 and was deleted per this file's own derive-it-don't-restate-it rule. The new span is chosen to contain no number, so it cannot be consumed the same way.
HAZARD | is a bash file — it stays in `SHELL_FILES` for `bash -n` and shellcheck — and is also the one deliberate entry in `ZSH_FILES` | - | - | -
HAZARD | (installs pre-commit and pre-push hooks; run once per checkout) | - | - | -
HAZARD | (regenerates `.cursor/rules/global-claude-standards.mdc` from root `CLAUDE.md`'s `@~/.claude/standards/*.md` imports, resolved against the global symlinked standards dir) | - | - | -
HAZARD | (fails when generated Cursor guidance is stale) | - | - | -
HAZARD | before every sync, and that file is the only rollback path.** `run_update` writes `pip freeze` to `~/.local/share/dotfiles/venv-snapshots/ansible-<UTC>.txt` before applying the lock, keeping the newest 10. | - | - | -
HAZARD | is required — the state being restored is one the resolver refuses. | - | - | -
HAZARD | added by the uv work.** All three exist for a stated reason and none grants a capability the operator did not already have: | - | - | -
HAZARD | operator escape hatch, and the only seam a test can use to drive the "not executable" branch — PATH mocking cannot remove the absolute fallback candidates | - | - | -
HAZARD | (renders **all five** CI requirements files from `uv.lock`) | - | - | -
HAZARD | renderings, deliberately separate files.** | - | - | -
HAZARD | the full local/dev test set | - | - | -
HAZARD | asserts distinctness across two tests: "the two renderings are different files with different content" (`test-lint` vs `runtime`) and "all four renderings are distinct files" | - | - | -
HAZARD | and that shape was measured and rejected rather than skipped.** Dropping every genuinely-uninvoked tool takes the test-lint rendering 80 → 73, so a consumer running three tools still installs 70 it never runs. | - | - | -
HAZARD | because it was wrong for most of the design: this was never a deletion problem.** The packages are legitimately in the venv — a human might use any of them | - | - | -
HAZARD | is stated in `pyproject.toml` and guarded by a test, not by review.** The predicted failure is erosion — a repo needs one more tool, it lands in `ci-test` because that is where tools go | - | - | -
HAZARD | stay in `test-lint` and are absent from every CI group, which is the point.** `bandit` is invoked by `security-review/SKILL.md:110` — a Phase 3 gate that runs on a developer machine | - | - | -
HAZARD | does not go in these headers, and that is load-bearing.** A `runtime`-group edit moves `uv.lock` without changing the `test-lint` export | - | - | -
HAZARD | cannot see a wrong-group declaration.** It verifies each rendering is faithful to its group; a package in the wrong group renders faithfully and passes. | - | - | -
HAZARD | is a **rendering, not a declaration** — `pyproject.toml` plus `uv.lock` are the source. It exists so the other repos' CI can `pip install -r` it with stock pip and no `uv` on the runner | - | - | -
HAZARD | is not byte-deterministic** — its header echoes the argv it was given, including an absolute `--project` path, so `scripts/sync-requirements-ci.sh` strips that header and writes a fixed one | - | - | -
HAZARD | hook is **required**. It runs on every `git commit`: | - | - | -
HAZARD | — blocks the commit on any syntax or shellcheck failure | - | - | -
HAZARD | — scans staged changes for secrets before they reach the remote. **Resolved by explicit override, then `PATH`, then absolute prefixes — not by `command -v` alone — and the skip is announced on stderr, never silent.** | - | - | Checked against shell.md/git-workflow.md ggshield material; no literal counterpart found — the ggshield actor-boundary resolution is dotfiles-specific narrative, not duplicated. Class HAZARD per instruction.
HAZARD | hook is **permanent**. It runs `make test` (lint + bats) on every push before the push reaches GitHub, and it **fails closed** | - | - | -
HAZARD | `scripts/pre-push` must resolve repo root with `git rev-parse --show-toplevel` first, and use `git rev-parse --git-common-dir` parent only as a fallback. | - | - | -
DUPLICATE-judgement | must `unset GIT_DIR GIT_WORK_TREE GIT_COMMON_DIR GIT_INDEX_FILE` before invoking `make test` | ~/.claude/standards/git-workflow.md | "put `unset GIT_DIR GIT_WORK_TREE GIT_COMMON_DIR GIT_INDEX_FILE` in every bats `setup()`" | CLAUDE.md (Testing section, "Git env strip requirement" bullet): "`scripts/pre-push` must `unset GIT_DIR GIT_WORK_TREE GIT_COMMON_DIR GIT_INDEX_FILE` before invoking `make test`, and that line must stay below the range-resolution loop, which legitimately needs the git environment." git-workflow.md ("The same leak corrupts the INDEX" entry, "How to apply"): "put `unset GIT_DIR GIT_WORK_TREE GIT_COMMON_DIR GIT_INDEX_FILE` in every bats `setup()` that creates a fixture repo — at setup scope, not per-test." Measured: phrase_check.match_phrase(normalize(git-workflow.md), the full declared phrase) = 0 — git-workflow.md's imperative ("put ... in every bats setup()") never matches CLAUDE.md's declarative ("scripts/pre-push must unset ... before invoking make test"). Shared: identical remedy (strip the four git repo-location vars before running tests). Diverges: git-workflow.md states the general bats-fixture rule from the ai-devops#6 incident (90 entries staged as deletions); CLAUDE.md applies it specifically to `scripts/pre-push`, naming the exact ordering constraint (below the range-resolution loop) and this repo's own measured incident (90 failures) plus the pinning test (`tests/scripts/pre_push.bats`). VERDICT: judgement. Nothing removed.
HAZARD | and that line must stay **below** the range-resolution loop, which legitimately needs the git environment. | - | - | This repo-specific ordering constraint is not in git-workflow.md's general rule — stays as HAZARD per instruction.
HAZARD | refuses a push whose `remote_ref` is `refs/heads/master` when the diff carries an executable-class path | - | - | -
HAZARD | is **deliberately narrower** than `sdlc-branch-guard.sh`'s `is_safe_file`, and the exclusions are chosen rather than overlooked | - | - | -
HAZARD | constraints, both load-bearing. The verdict **accumulates in a flag inside the stdin loop and is read after it**, never `exit`ing mid-loop | - | - | -
HAZARD | evaluates the push RANGE, not the tip commit, and the first person to hit that will read it as a false positive.** | - | - | -
HAZARD | is itself executable-class, so a defective guard cannot be repaired by a direct push to master.** | - | - | -
HAZARD | in `tests/scripts/pre_push.bats` push to a feature ref deliberately — do not switch them back to master.** | - | - | -
HAZARD | job (gitleaks) is a backstop, not a substitute for local scanning. Install ggshield: `brew install gitguardian/tap/ggshield && ggshield auth login`. | - | - | -
HAZARD | at the repo root carries **one** suppression, `SC1091`, and the file is byte-identical across the four repos that use the shared minimal config. | - | - | -
DUPLICATE-judgement | is structurally unavoidable rather than a preference: the lib architecture resolves source paths at runtime through `$(dirname "${BASH_SOURCE[0]}")`, which does not exist at lint time. | ~/.claude/standards/shell.md | "not yet written. `SC1091` (source path not found) qualifies: the lib architecture resolves..." | CLAUDE.md (ShellCheck section, `.shellcheckrc` paragraph): "`SC1091` is structurally unavoidable rather than a preference: the lib architecture resolves source paths at runtime through `$(dirname "${BASH_SOURCE[0]}")`, which does not exist at lint time." shell.md ("`.shellcheckrc` carries only structurally unavoidable suppressions" entry): "`SC1091` (source path not found) qualifies: the lib architecture resolves source paths at runtime through `$(dirname "${BASH_SOURCE[0]}")`, which cannot exist at lint time." Measured: phrase_check.match_phrase(normalize(shell.md), the full declared phrase) = 0 — the two differ by one word ("does not exist" vs "cannot exist"). Shared: identical rationale for the suppression, near word-for-word. Diverges: shell.md states this as the fleet-wide rule, contrasted against the SC2086 blanket-disable anti-pattern with a site-suppression example; CLAUDE.md applies the conclusion to dotfiles' own `.shellcheckrc` inventory ("carries one suppression ... byte-identical across the four repos", `etch-cli`'s stricter layering). VERDICT: judgement — a one-word wording difference is still a paraphrase under this format's literal-match gate. Nothing removed.
HAZARD | the previous convention, deliberately.** Until 2026-08-08 the file blanket-disabled `SC2086`, `SC2034`, `SC1091` and `SC2181`, and this section described unquoted variables as "intentional style throughout." | - | - | -
HAZARD | was not free. One of the four `SC2181` sites it covered was a real defect: `install_homebrew` ran `xcodebuild -license accept` and `xcodebuild -runFirstLaunch` back to back and then tested `$?` | - | - | -
HAZARD | for any new suppression: | - | - | -
HAZARD | carries a reason on the same line**, and the reason names the mechanism — for `SC2034`, the file and function that consume the variable, not "used elsewhere." | - | - | -
HAZARD | to suppressing.** A variable nothing reads is dead code; annotating it makes it permanent. | - | - | -
HAZARD | is live before writing its reason.** Delete it, re-run shellcheck, and confirm its finding returns. | - | - | -
DUPLICATE-judgement | in a file is file-wide**, not scoped to the next command — verified against shellcheck 0.11.0. | ~/.claude/standards/shell.md | "A bare directive before the first non-comment command is file-wide, not next-command" | CLAUDE.md (ShellCheck section bullet): "A bare directive before the first non-comment command in a file is file-wide, not scoped to the next command — verified against shellcheck 0.11.0." shell.md (heading + reproduction): "A bare directive before the first non-comment command is file-wide, not next-command" plus the two-case `bash -x` reproduction (0 findings file-wide vs 2 findings next-command-only). Measured: phrase_check.match_phrase(normalize(shell.md), the full declared phrase) = 0 — CLAUDE.md's "not scoped to the next command" paraphrases shell.md's own heading wording "not next-command". Shared: identical finding (verified on shellcheck 0.11.0). Diverges: shell.md carries the runnable reproduction script; CLAUDE.md states the conclusion and names this repo's own deliberate users of the file-wide form (`lib/constants.sh`, `config/profiles.sh`) — shell.md's parallel example is a generic constants file, not these two. VERDICT: judgement. Nothing removed.
DUPLICATE-judgement | rejects a directive on a `case`-arm line** (`SC1124`). It must precede the whole `case`, which means it covers every arm | ~/.claude/standards/shell.md | "`SC1124` compounds it: a directive cannot sit on a `case`-arm line, so it must precede the whole `case`..." | CLAUDE.md (ShellCheck section bullet, immediately below the file-wide-directive bullet): "shellcheck rejects a directive on a `case`-arm line (`SC1124`). It must precede the whole `case`, which means it covers every arm — state the blast radius when you write one." shell.md (same entry, "SC1124 compounds it" sentence): "`SC1124` compounds it: a directive cannot sit on a `case`-arm line, so it must precede the whole `case` and therefore covers every arm." Measured: phrase_check.match_phrase(normalize(shell.md), the full declared phrase) = 0 — CLAUDE.md opens with "shellcheck rejects" where shell.md opens with "SC1124 compounds it"; different sentence shape around the identical fact. Shared: SC1124 forces the directive to precede the whole `case`, covering every arm. Diverges: shell.md frames SC1124 as a compounding factor of the file-wide-directive hazard immediately above it; CLAUDE.md states it as a standalone bullet and folds in shell.md's own separate "How to apply" instruction ("state the blast radius") as a direct imperative attached to this specific rule. VERDICT: judgement. Nothing removed.
DUPLICATE-judgement | scope comes from `scripts/list-shell-files.sh`, not a literal list, not `find`, and not a `git ls-files` pathspec. A pathspec is extension-keyed and cannot express "every tracked shell script" | ~/.claude/standards/shell.md | "A `git ls-files` pathspec cannot express \"every tracked shell script\"" | CLAUDE.md (ShellCheck section, opening paragraph): "`make lint`'s scope comes from `scripts/list-shell-files.sh`, not a literal list, not `find`, and not a `git ls-files` pathspec. A pathspec is extension-keyed and cannot express "every tracked shell script" — it silently drops the extensionless hooks and every mock under `tests/mocks/`." shell.md (heading + body, "A `git ls-files` pathspec cannot express..." entry): "Pathspecs are extension-keyed. `git ls-files '*.sh' '*.bash'` matches no extensionless file, so every git hook — `pre-commit`, `pre-push`, `commit-msg` — is invisible to it." Measured: phrase_check.match_phrase(normalize(shell.md), the full declared phrase) = 0 — CLAUDE.md's sentence names `scripts/list-shell-files.sh` and `find` inline, which shell.md's general entry never does. Shared: identical argument — an extension-keyed pathspec cannot express "every tracked shell script" and silently drops extensionless files. Diverges: shell.md states the fleet-wide finding (etch-cli 1/4, ai-config 28/29, terraform_ansible 15/18) with the Wrong/Correct `git ls-files` code block; CLAUDE.md names the actual mechanism this repo chose instead (a shebang-derived scan) plus a dated self-correction (stale `101`/`102` figures until 2026-09-08) and the live count command. VERDICT: judgement. Nothing removed.
HAZARD | covers `tests/mocks/`, which it did not until this branch.** All 64 mocks tracked at that time were extensionless | - | - | -
HAZARD | does NOT set OS vars, and this bullet said the opposite until 2026-09-10.** It sources `setup_env.sh`, which _defines_ `detect_env` via `lib/detect_env.sh` but returns at its sourcing guard | - | - | -
HAZARD | tests stay off real pip because `load_mocks` exports `MOCK_PYENV_WHICH_STDOUT` by default** (`tests/helpers/common.bash:13`). | - | - | -
HAZARD | in `setup_env.sh` must have a test in `tests/setup_env/unit.bats` (pure logic) or `tests/setup_env/install_guards.bats` (side effects requiring mocks) | - | - | -
HAZARD | to an existing function must update its test | - | - | -
HAZARD | get their own directory under `tests/` (e.g., `tests/scripts/`) | - | - | -
HAZARD | real system state in tests — use PATH-based mocks from `tests/mocks/` | - | - | -
HAZARD | must exit 0 before committing | - | - | -
HAZARD | for PowerShell coding and testing standards. Run tests in this repo from the `powershell/` directory: | - | - | -
HAZARD | (one-time): | - | - | -
HAZARD | 90%. `make test` and CI both fail on any drop below the floor. | - | - | -
HAZARD | `setup_windows.ps1` only. `run-tests.ps1` and `run-lint.ps1` are excluded as test/lint glue (per tdd.md "entry-point glue that purely calls already-tested functions"). | - | - | -
HAZARD | is `setup_env.sh` plus tracked `config/*.sh`, `lib/*.sh`, `scripts/*.sh` and the two extensionless hooks (`scripts/pre-push`, `scripts/commit-msg`), derived from `git ls-files` at run time, less `scripts/bash-tracer.sh`. | - | - | -
HAZARD | was outside the set until 2026-08-09, and the stated reason for that was wrong.** This bullet used to read "nothing under test sources them, so instrumenting them would add only zeros to the denominator." | - | - | -
HAZARD | is the sole remaining exclusion, and it is measured rather than asserted.** `set -x` is its last command, so nothing before it can be traced and nothing follows it to trace. | - | - | -
HAZARD | rather than a filesystem glob is load-bearing, not stylistic.** `config/local.sh` is machine-local and git-ignored, but it exists on developer machines and not on a CI runner | - | - | -
HAZARD | counts commands, not source lines.** bash xtrace emits one line per _command_, so any construct where one command spans several lines inflates the count with lines no test can ever reach. | - | - | -
HAZARD | forms of all four still count. | - | - | -
HAZARD | was tried and removed on evidence, not preference.** The heuristic once dropped lines like `detect_env() {` from the coverable count on the theory that bash doesn't consistently trace them. | - | - | -
HAZARD | is the union of the static heuristic's coverable-line count and whatever the trace file actually contains for that file, never the heuristic alone.** | - | - | -
HAZARD | is now a hard, loud non-zero exit — replacing a silent clamp that had been hiding real over-matches.** | - | - | -
HAZARD | one file's denominator, or a full run's coverage against a real trace, without waiting on the bats suite: | - | - | -
HAZARD | CI's bash coverage figure in the PR body once CI has run (`gh pr edit <n>`); a local run is a preview, labelled as one. | - | - | -
HAZARD | via PS4 xtrace (`scripts/run-bash-coverage.sh`). | - | - | -
HAZARD | or publishing a bash coverage figure, or editing `scripts/run-bash-coverage.sh` or `scripts/bash-tracer.sh`, read `~/git-repos/personal/ai-config/docs/knowledge/dotfiles-bash-coverage.md` | - | - | -
REFERENCE | for the full `MOCK_*` env var reference table and the usage pattern. | - | - | -
DUPLICATE-judgement | so tests that assert actual filesystem state work correctly. Set the corresponding exit var to a non-zero value to simulate failure instead. | ~/git-repos/personal/ai-config/docs/knowledge/dotfiles-bats-test-infrastructure.md | "Pass-through mocks:" (`ln`, `chmod`, `mv`, `cp`, `tee`) | CLAUDE.md (Mock Pattern section): "Pass-through mocks: `ln`, `chmod`, `mv`, `cp`, and `tee` call the real binary (`/bin/cmd "$@" 2>/dev/null || true`) so tests that assert actual filesystem state work correctly. Set the corresponding exit var to a non-zero value to simulate failure instead." ai-config dotfiles-bats-test-infrastructure.md ("Pass-through mocks:" entry): "`ln`, `chmod`, `mv`, `cp`, and `tee` call the real binary (`/bin/cmd "$@" 2>/dev/null || true`) so tests that assert actual filesystem state (permissions, file existence, symlinks, captured output files) work correctly. Set the corresponding exit var to a non-zero value to simulate failure instead. Any mock that needs to support tests checking real filesystem state must use this pattern — a log-only mock will cause silent assertion failures." Measured: phrase_check.match_phrase(normalize(the ai-config doc), the full declared phrase) = 0 — the doc inserts a parenthetical ("permissions, file existence, symlinks, captured output files") between "state" and "work correctly" that CLAUDE.md's copy omits, breaking the literal match. Shared: near word-for-word restatement of the pass-through-mock list and behavior. Diverges: the ai-config doc adds that parenthetical and a generalizing final sentence ("Any mock that needs to support tests ... a log-only mock will cause silent assertion failures") that CLAUDE.md's copy lacks; CLAUDE.md's own next line (`env -i` subprocess strips PATH) already points a reader at this exact doc for detail, so this bullet sits one line above a pointer to itself. VERDICT: judgement — the closest of this task's rows to a literal duplicate (one inserted clause away) but still fails --deleted-have-counterparts. Nothing removed.
DUPLICATE-judgement | subprocess strips PATH** — `setup_ansible()`'s pyenv calls need the mock placed at `${HOME}/.pyenv/bin/pyenv`, not PATH-injected. | ~/git-repos/personal/ai-config/docs/knowledge/dotfiles-bats-test-infrastructure.md | `## \`env -i\` Subprocess Strips PATH — pyenv Mock Placement` | CLAUDE.md (Mock Pattern section, next bullet): "`env -i` subprocess strips PATH — `setup_ansible()`'s pyenv calls need the mock placed at `${HOME}/.pyenv/bin/pyenv`, not PATH-injected." ai-config doc ("## `env -i` Subprocess Strips PATH — pyenv Mock Placement"): "`setup_ansible()` on Linux uses `env -i ... bash -lc '... pyenv install ...'` with a stripped environment. PATH-injected mocks in `tests/mocks/` are invisible to this subprocess because `env -i` clears `PATH`. To intercept pyenv calls in these tests, create the mock directly at `${HOME}/.pyenv/bin/pyenv` (the hardcoded path pyenv resolves to):" Measured: phrase_check.match_phrase(normalize(the ai-config doc), the full declared phrase) = 0 — CLAUDE.md compresses the whole mechanism into one clause ("subprocess strips PATH ... need the mock placed at ... not PATH-injected") where the doc states it as three separate sentences (the invocation shape, the reason PATH-injected mocks are invisible, and the mock-placement instruction), never assembled into CLAUDE.md's exact wording. Shared: identical conclusion and identical target path (`${HOME}/.pyenv/bin/pyenv`). Diverges: the doc gives the actual `env -i ... bash -lc` invocation, a `mkdir`/`cp`/`chmod` code snippet, and generalizes ("This pattern applies to any function that strips the environment and invokes pyenv by absolute path"); CLAUDE.md's copy is terser and duplicates the doc's own summary rather than only pointing to it, though it does also name the doc as the detail pointer in the same sentence. VERDICT: judgement. Nothing removed.
HAZARD | parses short-option clusters, not just bare `-o`/`--fail`.** Production calls curl as `-fsS -o <file> <url>` and `-fLo <file> <url>` | - | - | Checked against shell.md's curl/-K credential-handling entry; different subject (short-option cluster parsing vs credential escaping). No counterpart. Class HAZARD per instruction.
HAZARD | write is now deferred until after the exit code is decided, and it is a deliberate deviation from real curl.** | - | - | Checked against shell.md's curl entries; no counterpart discusses the mock's deferred -o write semantics. Class HAZARD per instruction.
# Phrase manifest -- fragment C (Task 5)
#
# Covers: Test Seams, MAKEFLAGS and Stdout Partition, and every CLAUDE.md section not
# owned by the sibling agents (see Layout, Symlink Strategy, Key Conventions, Testing
# intro, ShellCheck, Testing Rules, PowerShell Testing, Coverage, Mock Pattern -- those
# are covered by fragments A/B).
#
# class | phrase | counterpart-file | counterpart-symbol | note

AMBIGUOUS | bootstrapping system for macOS and Linux (Ubuntu) | - | - | Plain orientation description; fits none of the four classes — necessary context, not a hazard, duplicate, dated record, or reference scaffold.
AMBIGUOUS | the 10-80-10 execution cycle defined in `ai-config` ADR-0009 | - | - | Names the governing ADR without restating its content; not itself a hazard/duplicate/record/reference claim.
RECORD-deleted | this line named five steps with `pr-review` first until 2026-09-10 | - | - | Deleted: "Read the order there rather than here" already tells the reader not to trust this file's ordering; the dated correction adds only provenance. Removed from CLAUDE.md.
AMBIGUOUS | a plan before dispatch: | - | - | Connector sentence introducing the code block that follows; not a substantive claim in any of the four classes.
AMBIGUOUS | make validate-plan PLAN=docs/superpowers/plans/<file>.md | - | - | Example command inside a code fence; plain usage, not a special class.
AMBIGUOUS | enforces required fields, valid role/model/tdd values, haiku scope guard | - | - | Plain description of what the validator checks; no non-obvious consequence, dated correction, or duplication identified.
DUPLICATE-deleted | docs/knowledge/README.md` for the master index | /home/bruce/.claude/standards/repo-structure.md | Knowledge Directory pointer-stub rule | Verified via whitespace-normalised longest-common-substring: 65-byte literal overlap with the global standard's own knowledge-directory pointer-stub sentence. VERDICT: deleted — the full sentence "See `ai-config/docs/knowledge/README.md` for the master index." is byte-identical in both files (phrase_check.match_phrase: 1 occurrence in each). Removed that sentence from CLAUDE.md's Knowledge Directory paragraph; the preceding sentence ("The local `docs/knowledge/README.md` is a pointer stub.") already names the file that itself carries the link, so nothing is orphaned.
DUPLICATE-partial | context-mode fetches produce findings worth preserving | /home/bruce/.claude/standards/repo-structure.md | Knowledge Directory save-instruction rule | Verified via LCS: 58-byte literal overlap with the global standard's save-to-ai-config instruction. VERDICT: partial — the matched fragment sits inside two differently-worded sentences: CLAUDE.md names the web-research skill and the instantiated `dotfiles-<topic>.md` filename and says "save to"; repo-structure.md is the generic `<repo>-<topic>.md` rule and says "write the file directly in ... under the appropriate name", with "across sessions" instead. phrase_check.match_phrase finds 0 occurrences of either full sentence in the other file. Nothing removed.
AMBIGUOUS | ./setup_env.sh -t <type> | - | - | Code-fence usage synopsis; plain reference, not a special class.
HAZARD | which marketplaces to register, which plugins to install — comes from ai-config's `~/.claude/settings.json` | - | - | Non-obvious: the plugin manifest is not hardcoded here, it is read from another repo's settings file; a partial install result warns rather than aborting.
AMBIGUOUS | `setup` — Full machine setup (setup_user + all apps) | - | - | Plain flag reference for setup/developer/ansible entry types; no hazard or duplication content.
HAZARD | with the same command; if the original invocation carried `--venv-name`, repeat it | - | - | Non-obvious SIGINT/SIGTERM abort behaviour and its recovery path for recreate-venv; the bolded abort clause is repeated near-verbatim in the recreate-ruby bullet below (an internal echo within this same file, not a cross-file duplicate, since DUPLICATE requires 'another file').
HAZARD | reusing `install_ruby()` | - | - | Same SIGINT/SIGTERM abort hazard as recreate-venv, applied to the pinned Ruby reinstall path.
HAZARD | preserving the deliberate rc-2 -> 0 mapping in `git-repos`, `legacy-rsync`, `git-hooks` and `cargo-tools` | - | - | Non-obvious exit-code contract: a WARN section still exits 0, and the section-name list is deliberately absent from the prose after a hand-kept list went stale once.
AMBIGUOUS | Active health checks: identity-table load, symlinks, tool presence | - | - | Plain description of what `-t doctor` checks.
AMBIGUOUS | Compare pinned versions in `lib/constants.sh` against GitHub latest | - | - | Plain description of `-t check-versions`.
AMBIGUOUS | *Options:** | - | - | Bare two-word section divider preceding the --dry-run bullet; no content of its own. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
RECORD-withdrawn | and the earlier wording here ("no operation that leaves this machine") was false | - | - | Withdrawn: this is a safety-relevant guarantee (what --dry-run does and does not touch); quoting the exact retracted claim verbatim guards against a stale mental model (or a stale cached copy) being treated as more correct than the current text. No date is present, so it is not a rotting figure. Nothing removed.
AMBIGUOUS | PR review, issue management, repo browsing, diff access — across all projects | - | - | Plain feature description of the globally-configured GitHub MCP server.
AMBIGUOUS | run `setup_env.sh -t setup_user` after adding the token | - | - | Plain setup instruction; no non-obvious consequence or duplication identified.
AMBIGUOUS | PR diffs and changed files - Reading and creating issues - Posting structured review comments - Browsing repo contents | - | - | Plain bullet list of intended uses; not a hazard/duplicate/record/reference claim.
HAZARD | not use it to push directly to main/master — normal PR workflow still applies. | - | - | Guardrail reminder specific to this tool (an operator could otherwise assume an MCP write path bypasses the branch/PR requirement); not a literal-text duplicate of git-workflow.md (checked via LCS, no match >=20 bytes), so it is shared evidence for the same conclusion rather than a duplicate. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
HAZARD | so that BATS tests can mock `which` through PATH injection | - | - | Non-obvious: `command -v` is a builtin and ignores PATH mocks, so setup_env.sh deliberately uses `which` here and nowhere else.
RECORD-partial | The only `exit 0` paths are `:94` and `:103` | - | - | Partial: the parenthetical also carried "This read 'terminates cleanly at line 78 (`exit 0`)' until 2026-09-08" (pure provenance) but the `:103` exit point is real, load-bearing content not restated anywhere else in the paragraph. CLAUDE.md now reads "(The only `exit 0` paths are `:94` and `:103`.)" — keeps the fact, drops the dated correction framing. Re-anchored: the original phrase's leading parenthesis made it sentence-initial once isolated; re-pointed at the surviving fact minus the paren (CLAUDE.md, count 1, not sentence-initial).
HAZARD | conditional installs require non-zero exits to continue | - | - | States why `set -euo pipefail` is deliberately absent at top level.
RECORD-deleted | cited `legacy_rsync.sh:28`, and claimed the harness requires it, until 2026-09-10 | - | - | Deleted: exact match for the plan's example shape ("This bullet said X, cited Y, and claimed Z, until date"); the corrected rule (6 of 13 lib files, re-count command given) is already fully stated adjacently. Removed from CLAUDE.md.
RECORD-deleted | it said "replicated consistently across `setup_env.sh`" until 2026-09-10 | - | - | Deleted: the current claim ("illustrative rather than copied from any file") stands without the parenthetical naming the old wrong claim. Removed from CLAUDE.md.
AMBIGUOUS | if [[ -n ${MACOS} ]]; then ... | - | - | Illustrative code-fence example; not itself a claim.
AMBIGUOUS | the established helper functions, don't call `brew` directly: | - | - | Plain style instruction, no hazard/duplicate/record content. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
AMBIGUOUS | brew_formula_installed <formula> | - | - | Code-fence usage example; plain reference.
AMBIGUOUS | can be tagged with a trailing `# [HAS_*]` comment to make them profile-aware | - | - | Plain description of the tagging convention before the hazard paragraphs that follow.
AMBIGUOUS | brew "postgresql@14" # [HAS_DEVTOOLS] | - | - | Illustrative Brewfile snippet; plain example, not a claim.
AMBIGUOUS | that is developer-, K8s-, Docker-, or Rust-specific, add the appropriate tag | - | - | Plain style instruction.
HAZARD | does NOT gate installation, and reading it as a gate is the natural mistake | - | - | Core hazard of this section: every mac installs every main-Brewfile entry regardless of its [HAS_*] tag; the tag only changes drift-check expectations.
HAZARD | 6 of the 8 hostname entries carry `devtools`, so that is the blast radius of adding a `[HAS_DEVTOOLS]` entry | - | - | Non-obvious scope: a new tagged entry's advisory drift line reaches most machines, not the 1-2 an author usually has in mind.
HAZARD | new third-party taps must also be added to the `brew trust` call in `install_macos_casks`/`_install_ubuntu_brew_packages` | - | - | Operational rule to avoid breaking brew bundle symlinking / tap trust; points to a knowledge doc for full rationale.
AMBIGUOUS | for the full PowerShell coding and testing standards | - | - | Plain pointer sentence to the global standards file; no hazard/duplicate/record content of its own.
REFERENCE | links ai-config into native Windows alongside WSL2 | - | - | Windows/WSL2-only onboarding scaffolding; the Windows 7900X is USER.md's 'backup-of-last-resort' machine, so a session on any of the seven actively-developed machines never reads this to do its work.
REFERENCE | `skills/`, `commands/`, `standards/` as junctions | - | - | Same Windows-only onboarding scope as the section's other bullets — irrelevant outside the backup-of-last-resort machine.
REFERENCE | `.claude/hooks/` bash scripts are not linked on native Windows | - | - | Documents a Windows-only limitation; scaffolding for a machine that is not part of the active development set.
REFERENCE | pulls the latest ai-config (`Install-AiConfig`) and updates npm globals | - | - | Describes the Windows update entry point; same backup-of-last-resort scope as the rest of this section.
REFERENCE | admin terminal (symlinks need elevation), `GITHUB_PAT` env var for MCP config, Node.js (installed via Chocolatey `nodejs`) | - | - | Windows-only prerequisite list; onboarding scaffolding outside the active development set.
AMBIGUOUS | tool versions are defined as constants in `lib/constants.sh`: | - | - | Connector sentence introducing the duplicated code block below. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
DUPLICATE-deleted | GO_VER="1.27" | lib/constants.sh | GO_VER assignment | Byte-identical literal, confirmed by direct grep of lib/constants.sh; the very next CLAUDE.md sentence warns to keep it updated, conceding the copy cannot maintain itself. VERDICT: deleted — all four assignments in the fenced block (`GO_VER`, `PYTHON_VER`, `RUBY_VER`, `RUSTUP_VER`) are byte-identical to lib/constants.sh, confirmed by direct grep. Replaced the four literal lines with a one-line pointer inside the same fence naming lib/constants.sh and the four variables, so the block cannot go stale a second time; kept the fence rather than prose to avoid making the adjacent AMBIGUOUS phrase "Update these constants when bumping versions" newly sentence-initial.
DUPLICATE-deleted | PYTHON_VER="3.14.6" | lib/constants.sh | PYTHON_VER assignment | Byte-identical literal in lib/constants.sh, now asserted by --deleted-have-counterparts rather than left to review. Added after pr-review cycle 2 measured that the four-value Version Pinning deletion carried one row, so the gate verified 1 of 4 and the other three rested on a reviewer's grep.
DUPLICATE-deleted | RUBY_VER="4.0.5" | lib/constants.sh | RUBY_VER assignment | Byte-identical literal in lib/constants.sh, now asserted by --deleted-have-counterparts rather than left to review. Added after pr-review cycle 2 measured that the four-value Version Pinning deletion carried one row, so the gate verified 1 of 4 and the other three rested on a reviewer's grep.
DUPLICATE-deleted | RUSTUP_VER="1.29.1" | lib/constants.sh | RUSTUP_VER assignment | Byte-identical literal in lib/constants.sh, now asserted by --deleted-have-counterparts rather than left to review. Added after pr-review cycle 2 measured that the four-value Version Pinning deletion carried one row, so the gate verified 1 of 4 and the other three rested on a reviewer's grep.
AMBIGUOUS | these constants when bumping versions — don't hardcode versions elsewhere. When a constant is updated, update all other references to that constant ac | - | - | Plain maintenance instruction; not a hazard/duplicate/record/reference claim on its own. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
RECORD-deleted | this said `lib/macos.sh`, which has none, until 2026-09-10 | - | - | Deleted: the current rule ("chruby on macOS (installed by `Brewfile`, loaded by `.config/.zshrc.d/5_general.zsh`)") is unaffected by dropping the note about which file it used to (wrongly) cite. Removed from CLAUDE.md.
AMBIGUOUS | supplement the universal standards loaded | - | - | Plain orientation sentence naming what the imported standards add to.
AMBIGUOUS | standards/powershell.md ## Testing | - | - | Import directive, not a prose claim; functions as scaffolding wired into the CLAUDE.md loader.
AMBIGUOUS | runs on PRs to master only (the pre-push hook gates branch pushes locally) | - | - | Plain orientation sentence introducing the per-job bullet list.
HAZARD | verifies test count >= 840 (regression proxy) | - | - | Non-obvious: the test job's own regression guard is a raw count assertion rather than a coverage figure.
HAZARD | the `tr` is load-bearing, since `print-%` emits one space-separated line and `xargs -I` implies `-L1` and does not split on blanks | - | - | Non-obvious shell-plumbing detail in the lint-macos job; removing the `tr` would silently break the bash -n sweep.
HAZARD | the red-suite guard correctly refused to compute coverage over a red run — so the symptom was a coverage job failing on something that is not coverage | - | - | Incident (#226): bash-coverage runs the identical suite as `test` in a separate job, so every tool the suite depends on must be installed in both places.
HAZARD | a leaked credential must not auto-merge, so it fails closed here | - | - | Deliberate deviation from ci.md's Personal Repos clause, which treats secret-scan as advisory/non-blocking elsewhere.
RECORD-deleted | the code was right and the prose was wrong | - | - | Deleted: "there is no advisory tier" is already stated as the current rule two sentences earlier; the retraction of the old "advisory" prose adds only provenance. Removed from CLAUDE.md.
HAZARD | a hung job can no longer block auto-merge for GitHub's 360-minute default (PR #223 hit this live — `powershell` stalled 30m21s inside `Install PowerShell`) | - | - | Incident-driven rationale for the per-job timeout-minutes caps and their sizing.
AMBIGUOUS | requirements: - All jobs run on `ubuntu-latest` | - | - | Bare section-divider sentence introducing the final two bullets.
RECORD-deleted | actions/checkout@v5 | - | - | Deleted per plan: the "- Uses `actions/checkout@v5`" bullet under CI requirements was removed outright rather than corrected to v6/v7 — the real pin is `.github/workflows/ci.yml`'s SHA digest (currently `# v7`), and a floating version claim in CLAUDE.md will only go stale again.
AMBIGUOUS | for the full override env var table (moved to ai-config per ADR-0020) | - | - | Pointer sentence to the canonical table; not itself a hazard/duplicate/record claim.
AMBIGUOUS | and pass a writable temp copy; production code leaves it unset | - | - | States the generic seam template every following paragraph specialises; foundational rather than an individual hazard.
HAZARD | every test runs against a fake `HOME` with no `~/.cargo/bin/rustup`, so all of them would otherwise enter the install path and reach the network | - | - | tdd.md E2 hazard: without this seam group every rustup test would reach the real network.
HAZARD | There is no `sha256sum` mock and there must not be one | - | - | Deliberate non-seam: mocking the checksum verifier would make every mismatch assertion vacuous.
HAZARD | it writes `MOCK_CURL_STDOUT` to the `-o` target, or `touch`es it when unset | - | - | Explains why the digest test computes over the mock's own bytes rather than a separate fixture file it never copies.
HAZARD | the suite runs on a machine with no NVIDIA card, so the install branch is otherwise unreachable | - | - | Explains why _OVERRIDE_NVIDIA_GPU_PRESENT exists despite lspci itself being left unmocked.
HAZARD | the `sudo` mock's `command -v nvidia-ctk` otherwise resolves the **real** binary | - | - | tdd.md E2 hazard: without this pair the test would exec the real nvidia-ctk against the operator's live daemon.json; macOS cannot reproduce it since it has no nvidia-ctk at all.
HAZARD | an installed **cask** never matches there and the caller would reinstall it on every setup run | - | - | Idempotency-break hazard motivating a separate cask helper pair from the formula helpers.
HAZARD | a login+interactive shell runs it twice in one process | - | - | Explains why config/profiles.zsh is the single zsh-side derivation point and why it must tolerate double-sourcing.
HAZARD | a derived oracle follows the same table it is meant to check, so a hostname swapped onto the wrong legacy variable in `PROFILE_LEGACY` would agree with itself and pass silently | - | - | Core rationale for the deliberately hand-typed test oracle, citing behavior.md's same-decision-cannot-falsify principle; ADR-0021 carries the measured 2x2.
HAZARD | this file is linted by the same mechanism as the extensionless hooks, not because its `.bash` extension happens to match a pathspec | - | - | Explains why the shebang-derived lint scope picks up legacy_oracle.bash rather than a filename coincidence.
HAZARD | a `readonly` reassignment on the second pass makes that `source` return 126 | - | - | Explains the deliberate export/readonly asymmetry between config/profiles.zsh and lib/detect_env.sh.
HAZARD | the real prefix directories (`/opt/homebrew`, `/usr/local/opt`) exist on any provisioned mac, so a test that forgets to point these at a nonexistent path short-circuits the guard | - | - | Explains why _OVERRIDE_HOMEBREW_PREFIX_ARM/_INTEL must be driven through the override rather than the real filesystem.
HAZARD | a regression test could only fail on a machine that has keychain installed, which is neither CI nor macOS | - | - | Explains why _OVERRIDE_KEYCHAIN_BIN is required rather than optional for the keychain-binary seam.
HAZARD | zsh does not word-split unquoted parameter expansions | - | - | Explains why the unquoted keychain expansion was safe by accident, distinct from the (also true) threat-model argument given first.
HAZARD | both set `SH_WORD_SPLIT`, so any future code path that emulates another shell before sourcing this file gets splitting back | - | - | Explains why the expansion is quoted anyway even though the unquoted form happens to be safe today.
HAZARD | `[[ ]]` suppresses word splitting regardless of `SH_WORD_SPLIT`, so there is nothing to protect against | - | - | Distinguishes command-position quoting (needed) from test-position quoting (unnecessary churn) in the same file.
HAZARD | before the guard each source leaked an agent still holding the `bats-exec-suite` output pipe | - | - | Incident: 16 agents pinning the suite's pipe (161 accumulated since 2026-07-28) before the [[ -o interactive ]] guard was added around the keychain block.
HAZARD | reading the seam under a typo'd name leaves the negative test green and fails only the positive one | - | - | Mutation-confirmed property of the paired non-interactive/interactive keychain tests.
HAZARD | started from zsh read "already zsh" while the passwd entry still said `/bin/bash` | lib/helpers.sh | comment above _current_login_shell() | Paraphrases the same argument as the source comment at lib/helpers.sh:357-360 ("a provision run started from zsh reads 'already zsh' while the passwd entry still says /bin/bash -- the guard cannot see the thing it guards"); measured via the mandated python3 -c count command against lib/helpers.sh: 0. Per this task's own rule, 0 means NOT a duplicate, so this is HAZARD (shared evidence for the same conclusion, differently worded), not DUPLICATE -- corrected from an earlier draft that called it DUPLICATE on the strength of a manual reading rather than the command.
HAZARD | could not fire either, because the function's last command was `log_info` | - | - | This clause and the surrounding test/doctor narrative have no counterpart anywhere in lib/helpers.sh's comments (checked directly): the chsh/PAM sentence overlaps the source comment, but the majority of this paragraph -- the workflows.sh:153 detail, the three pre-existing tests, doctor's WARN rendering -- is original and belongs to HAZARD, not DUPLICATE.
HAZARD | keep the pairs in step, since a drift makes the install guard and the `PATH` consumer disagree | - | - | Cross-file/cross-language coupling hazard between lib/macos.sh's install_make_macos and 6_path.zsh's PATH prepend.
HAZARD | a stray export changes real shell `PATH`, which grants no capability beyond setting `PATH` directly but is worth knowing | - | - | Notes that the gnubin override pair is read unconditionally in production, not only under test.
HAZARD | a test asserts the two stay equal — a drift between them would make the doctor arm's diagnosis and the shell's actual `PATH` disagree | - | - | Same coupling hazard as the ARM gnubin pair, one platform over, for Linux coreutils.
HAZARD | that loses them is a non-interactive login shell (`zsh -l -c`) | - | - | Explains the deliberate narrowing of the Docker Desktop PATH entry from login shells to interactive ones, and which actors are and are not affected.
HAZARD | the installer's `.zprofile` lines reappear, delete them | - | - | Operational rule to prevent Docker Desktop's installer from re-widening the actor scope this seam deliberately narrowed.
HAZARD | an explicit override that silently falls back is worse than no override | - | - | States why GGSHIELD_BIN is a hard error rather than a degrade when non-executable.
HAZARD | the case named "ggshield is absent" ran the **real** ggshield on every dev machine and asserted nothing about the branch it named | - | - | Describes the pre-seam failure mode this test infrastructure was built to prevent.
HAZARD | `command -v` runs first and wins on any machine that already has a real `ledger` on `PATH` | - | - | Actor-specific (not machine-specific) resolution hazard: interactive zsh on workstation/claude resolves the real binary where a non-interactive actor on the same box would not.
HAZARD | un-allowlisted trap, allowlisted count, subshell trap still reported, count change, empty scope, unresolvable base | - | - | Explains why _OVERRIDE_LIB_TRAP_SCOPE globs a fixture root rather than the repo, avoiding a dirty tree mid-run for any concurrent session.
HAZARD | it redirects a read-only scan to a directory the caller can already read | - | - | States that the seam grants no capability beyond what it redirects.
HAZARD | certifies the glob path, while CI and the pre-push hook exercise the git path | - | - | Names the specific displaced-check gap in the lib-exit-trap scanner's own test coverage.
HAZARD | deliberately **above** the `mkfifo`, so the FIFO-reader deadlock it prevents is unreachable rather than merely unlikely | - | - | Explains the deliberate ordering of the bats pre-flight guard relative to the FIFO setup.
HAZARD | the workstation ran with the seam and CI ran the commit without it | - | - | Incident: a two-halves-of-one-seam bug shipped with the production half uncommitted in a worktree, reproducing as CI-only red for two hours because git stash create snapshots the working tree.
HAZARD | both select the parent directory the run's tmpdir is created under, and anyone who can export one can export the other | - | - | States that _OVERRIDE_RUN_TMPDIR_ROOT grants nothing beyond what TMPDIR already grants, and why bats' own TMPDIR handling makes the seam necessary anyway.
HAZARD | an environment-supplied value defeats the check entirely (measured: `env _PROFILES_LOADED=1 PROFILE=mac_workstation <the branch>` reports PASS) | - | - | Names the actual protection mechanism (the unconditional =0 on entry to detect_env) versus the non-export, which is not what makes the sentinel safe.
HAZARD | stripping `/opt/homebrew/bin` to make `gpg` absent takes `git` and `make` with it | - | - | Explains why the AWS verifier seams resolve by name rather than relying on a PATH strip — the delete-a-directory-to-delete-one-binary defect.
HAZARD | the command substitution returns **empty**, and the key resolves to `/keys/aws-cli-team.asc` | - | - | Incident (2026-09-07): a cwd-sensitive BASH_SOURCE[0] derivation silently pointed AWS signature verification at the filesystem root after update_aws_cli's own `cd`.
HAZARD | Sixty-seven `developer.bats` tests covered this code and none could fail for it, which is the durable half | - | - | tdd.md E3 / behavior.md actor-boundary instance: bats sources setup_env.sh by an absolute path, so the cwd-sensitivity bug was structurally unreachable from the suite.
HAZARD | a defect fixed in one of its two copies is not fixed | - | - | Notes the identical cwd-sensitive expression was duplicated into _doctor_check_aws_key_expiry and needed its own fix.
HAZARD | has a real `aws` at `/usr/local/bin/aws`, so without the seam every `install_aws_tools` test silently takes the already-installed branch | - | - | Sharpened absolute-path-default pitfall: the binary exists at the exact location a naive PATH strip would leave untouched.
HAZARD | none grants a capability the operator does not already have by editing `PATH` or the plist directly | - | - | States the general property of the whole cadence seam group before the per-variable table.
HAZARD | selects the `mktemp` used for the detector's stderr capture | - | - | One entry of the cadence override table (_RHN_MKTEMP): BSD mktemp with no template ignores TMPDIR, so the obvious seam is inert on every development mac.
AMBIGUOUS | "exit_code": 0, "findings": 0, "max_age_days": 8 | - | - | Illustrative JSON code-fence example of the heartbeat file shape; plain reference, not a claim.
HAZARD | A reader holding its own copy of the bound drifts silently: 8 against a writer that moved to 3 misses four days of staleness | - | - | max_age_days bullet: the heartbeat contract writes the bound rather than mirroring it, specifically to avoid this drift.
HAZARD | an **absent** heartbeat now means genuinely _not installed_ | - | - | pending bullet: distinguishes a seeded grace period from an absent heartbeat, and names setup_user as the migration path for pre-existing agents.
HAZARD | a reader rendering `held` as a fault would have passed the whole suite | - | - | held bullet: names why a pairwise-inequality test exists between the three heartbeat result classes.
HAZARD | the **clean path pushed `"findings": 1` on a fleet with zero drift** | - | - | findings bullet: the damaging half of the ai-config#227 incident — a stray stdout banner over-reports findings even when nothing is wrong, and was invisible from this repo because its only heartbeat sample came from a run that had real drift.
HAZARD | one missing interpreter would kill both channels at once — two channels that fail together are one channel | - | - | States why a python3 json.dumps emitter was considered and rejected for the heartbeat file.
HAZARD | A launchd agent sources no profile, so it gets `_PATH_STDPATH` unless the plist says otherwise | - | - | States the general PATH rule that motivates the cadence plist's explicit PATH entry.
RECORD-deleted | was missing until 2026-08-28 and the omission was not cosmetic | - | - | Deleted: the sentence that follows ("`ledger` lives there... so the repo already knew") already carries the WHY on its own; the dated "was missing until" framing was provenance. Removed from CLAUDE.md.
HAZARD | An interactive shell answers for a different actor and will tell you the tool is present | - | - | Instructs resolving a new detector's dependencies under the agent's own restricted PATH rather than a developer's interactive shell.
AMBIGUOUS | env -i PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin | - | - | Illustrative probe command; plain example, not a claim.
HAZARD | it reads `PATH` out of the **rendered** plist and requires a fixture binary | - | - | Explains why launch_agents.bats guards the PATH property rather than a fixed string.
HAZARD | merging a template change leaves every already-installed agent on its old contents until `setup_env.sh -t setup_user` re-installs it | - | - | Incident (dotfiles#248): a plist fix landed on master while the already-installed live agent still carried the pre-fix PATH.
AMBIGUOUS | grep -A1 '<key>PATH</key>' ~/Library/LaunchAgents/com.brucejackson.ledger-drift.plist | - | - | Illustrative verification command; plain example.
HAZARD | a credential containing a **newline is refused rather than escaped**, since curl's config format is line-oriented | - | - | States the ntfy credential-handling rule: stdin via curl -K -, never -u, and no escaping of embedded newlines.
HAZARD | a weekly "still alive" push trains the operator to ignore the channel and destroys the signal arm | - | - | Design rationale for the heartbeat being a silent-unless-stale second channel, contrasted against ai-config's ledger-drift.yml counter-example.
HAZARD | otherwise detection and delivery would fail together and "no drift" would render identically to "no channel" | - | - | States why install_ledger_drift_agent invokes its detector under env -u NTFY_URL.
AMBIGUOUS | the pyenv shim defect and the cargo plugin repair added 2026-09-17 | - | - | Sub-header sentence naming the seam group that follows; not itself a claim.
HAZARD | pyenv's own `init` exports it into every interactive shell, so a test's real `PYENV_ROOT` (if inherited) would outrank a fixture `HOME` | - | - | Explains why _OVERRIDE_PYENV_ROOT is required rather than a HOME-only redirect.
HAZARD | any suite calling `load_mocks` could fall through to a real `~/.cargo/bin/cargo` and compile all eight `CARGO_TOOLS` pins for real | - | - | tdd.md E2 hazard motivating the default-exported _CARGO_BIN mock.
HAZARD | a real, unwritable-looking `/usr/local/bin` still gets a real write once `sudo` execs through | - | - | Explains why stubbing sudo alone cannot mock _RELEASE_BIN_DIR/_TFENV_LINK_DIR's install paths.
HAZARD | BSD `mktemp -d` with a template argument ignores `TMPDIR` entirely | - | - | Explains why _RELEASE_TMP_ROOT exists on top of TMPDIR, mirroring the earlier _OVERRIDE_RUN_TMPDIR_ROOT fix.
HAZARD | mocking the checksum tool would make every mismatch case vacuous, since the mismatch is exactly the property under test | - | - | Explains why sha256sum stays real for the tflint/tfsec pinned-binary seams, mirroring the rustup rule.
HAZARD | both default to the same values `run_update`'s pre-existing `tfenv` `git pull` section already assumes | - | - | States the isolation purpose of _TFENV_ROOT/_TFENV_REPO_URL for the clone-failure test path.
HAZARD | the probe tests that `pwsh` *runs*, not merely that a `.deb` was downloaded | - | - | States what _PWSH_BIN/_PWSH_PROBE_TIMEOUT actually verify across both call sites of _pwsh_probe_runs.
HAZARD | replacing its whole body with `return 1` left all 117 tests in `linux_ubuntu.bats` green | - | - | Names the measured coverage gap in the timeout-absent fallback before the absolute-shebang stub seam was added.
HAZARD | a `pwsh` that hangs rather than exiting non-zero is exactly the half-installed state this function exists to repair | - | - | States what _DOCTOR_PROBE_TIMEOUT-style bounding protects against for the powershell probe.
HAZARD | the seam mirrors the URL-override pattern the tflint/tfsec pins already use | - | - | States the rationale for _CRATES_API existing alongside the tflint/tfsec URL-override precedent.
AMBIGUOUS | `_claude_manifest_split_line`, `setup_claude_plugins`, `provision_claude_plugins` | - | - | Sub-header sentence naming the function group covered by the Claude plugin provisioning seams; not itself a claim.
HAZARD | test runs under a redirected `HOME` with no settings file at all, so without this default they would all hit the manifest's rc 1 | - | - | Explains why load_mocks exports _OVERRIDE_CLAUDE_SETTINGS by default, pointed at a per-test copy rather than the tracked fixture.
HAZARD | `tests/mocks/claude`'s sibling, `tests/mocks/git`, answers almost every subcommand with empty stdout and exit 0 | - | - | Explains why _CLAUDE_GUARD_GIT resolves a real git with the mocks directory stripped from PATH, per shell.md's PATH-mock-shadows-production-binary pitfall.
HAZARD | `MOCK_CLAUDE_FAIL_ON_CALL=<n>` fails only the *n*th invocation whose argv is exactly `plugins list --json` | - | - | Describes one of five new tests/mocks/claude modes added for the plugin-provisioning seams, and what each drives.
HAZARD | `claude` would read from the loop's own `<<<"${_manifest}"` here-string and consume the remaining manifest lines | - | - | Explains why every claude call inside the manifest while-read loop redirects stdin from /dev/null.
HAZARD | the repo position is a literal `-` placeholder (`untracked\t-\t<path>`, `unknown\t-\t<path>`) rather than an empty string | - | - | Explains how _claude_settings_git_state avoids the IFS tab-collapse hazard shell.md documents, without using the split-line helper.
HAZARD | cannot heal itself, because its repair runs only when that MCP server starts | - | - | Incident: brew upgrade node deletes the versioned path context-mode wrote into its cache, and the fix requires the repair to run from outside the broken server.
HAZARD | prints `Entering directory` / `Leaving directory` on stdout when `-C` changes directory; macOS's `/usr/bin/make` is 3.81 and does not | - | - | States the version split that motivates the whole partition below.
HAZARD | with the directive 3 lines, without it 3 lines, byte-identical | - | - | Measured: MAKEFLAGS += --no-print-directory does not cover a direct `make -C` on GNU Make 4.3, since -C prints before the Makefile is parsed.
HAZARD | outside an outer make on 4.3 is uncovered | - | - | States what the exported MAKEFLAGS directive actually buys versus what it does not.
HAZARD | any test that captures and measures `make` output must explicitly account for it — tests fall into two categories | - | - | States the guarded/measuring partition requirement for every stdout-capturing make -C test.
HAZARD | on 4.3 it strips the one mechanism that works and leaves the inert file directive, so a case that merely wants an exact value must be **guarded**, not measuring | - | - | Explains why the measuring category (env -u MAKEFLAGS) is only for tests that genuinely need to observe directory lines, citing a shipped mistake caught by CI.
HAZARD | capturing `make` output without guarding or measuring it gets the environment's `MAKEFLAGS`, so it is measuring the environment rather than the Makefile | - | - | States the failure mode the guarded/measuring partition exists to prevent.
HAZARD | scans every stdout-capturing `make` invocation across the scanner's domain and requires each to land in exactly one of guarded/measuring, with both sets non-empty | - | - | Describes the enforcement mechanism (tests/scripts/makefile_lint_scope.bats) that makes the partition mandatory rather than aspirational.
HAZARD | hardcoded a two-file array (`tests/makefile_scope.bats` and this file) that held exactly the two files already in compliance and excluded the one real violation | - | - | Names the third recorded instance in this repo of tdd.md's Coverage Denominators invisible-omission shape.
HAZARD | both print `Entering`/`Leaving` with no `-C` anywhere on the line that triggers them, so neither is reachable by this scanner's `-C`/`--directory` pattern | - | - | Recorded, accepted boundary of the line scanner: recursive sub-make and -w are invisible to it, not exploitable today but not closed either.
DUPLICATE-partial | `caveman:caveman-commit` skill to generate the commit message before running `git commit`. Full format and rules in `~/.claude/CLAUDE.md`. | /home/bruce/.claude/CLAUDE.md | Committing Work section | Verified via LCS: 70-byte literal overlap with the identical instruction already given in the global CLAUDE.md, whose full format and rules this section explicitly points back to in its very next clause. VERDICT: partial — CLAUDE.md's full sentence ("...before running `git commit`.") is not the global CLAUDE.md's full sentence ("— do this before running `git commit`, not after."); phrase_check.match_phrase finds 0 occurrences of either full sentence in the other file, only the fragment ending at "commit message" is genuinely shared. Nothing removed. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
HAZARD | resolves `extends` at `initRepo`, _before any dependency extraction_, so an unfetchable preset throws `config-validation` and abandons the whole repository | - | - | Core hazard motivating inlining the shared preset rather than extending it, since ai-config is private and dotfiles is public.
RECORD-partial | pinned all six `actions/checkout` refs to `d23441a…` and **auto-merged** | - | - | Partial: deleted the "paragraph above could only say..." meta-commentary clause (the paragraph reads fine without it) AND, per the plan's explicit instruction, removed the stale `actions/checkout@v6`/`v7` version references from the same paragraph rather than correcting them to the current v7 digest pin — reworded to "actions/checkout refs" and "a major-version bump", keeping the durable content (#240/#241, packageRules behavior). Re-anchored: the deleted meta-commentary clause took the original phrase with it; re-pointed at the surviving #240 sentence in the same paragraph (CLAUDE.md, count 1, not sentence-initial).
HAZARD | Under silent mode no repo could ever author a PR, so zero was the only reachable value and every "it is not running" verdict built on it was unprovable | - | - | States the structurally-unfalsifiable-oracle lesson this repo's own history demonstrates.
HAZARD | renaming a slice to a single-segment name would silently bring it into scope | - | - | Explains why pip_requirements is deliberately absent from enabledManagers and why the five CI renderings' naming is load-bearing.
HAZARD | Verified on this repo and spot-checked on `math` and `state-ledger` | - | - | States the fleet-wide 2026-08-21 Dependabot decision this repo's file records because it lives nowhere else (a GitHub repo-settings toggle, not a tracked config file).
HAZARD | would not capture it either: that file governs _version_ updates, and these are _security_ updates | - | - | Explains why this repo-settings decision needs to be recorded in prose at all.
HAZARD | an internally consistent lock passes every CI check | - | - | States the #227 incident (an unreviewed Dependabot auto-PR) that motivates keeping alerts on and auto-PRs off.
HAZARD | **16 of 18 repos had vulnerability alerts switched off entirely** | - | - | States the fleet-wide measurement that inverted the day's original premise from over-automation to under-notification.
HAZARD | "Turn Dependabot off for Python" is not expressible; only the whole repo | - | - | Mechanical detail: automated-security-fixes has no per-ecosystem toggle.
HAZARD | enabling alerts later lights auto-PRs instantly | - | - | States why a repo with alerts off is latently armed for automated-security-fixes, and the required call order to avoid it.
HAZARD | Renovate manages no Python in any repo (`pep621` is enabled nowhere), including this one | - | - | States the resulting gap: no automated Python update path exists at all, despite pyproject.toml/uv.lock living in this repo.
AMBIGUOUS | must **never** be committed to this repo | - | - | Plain rule statement introducing the bullet list of machine-local paths; not itself a hazard/duplicate/record/reference claim.
HAZARD | ADR-0014 the `memory/` subdirectory is a session-local draft location | - | - | Explains the ~/.claude/projects/ exception and the invariant ai-config#138 added after 27 committed drafts.
AMBIGUOUS | copy from `config/local.sh.example`, git-ignored | - | - | Plain instruction for the local.sh override file.
AMBIGUOUS | add an allowlist entry to `.gitleaks.toml` | - | - | Plain instruction for handling a secret-scan false positive.
AMBIGUOUS | sourced by both bash (`lib/detect_env.sh`) and zsh | - | - | Plain orientation sentence introducing the capability table below.
HAZARD | `mac_workstation`, `mac_mini`, `linux_workstation`, `wsl2_workstation`, or `unknown` | config/profiles.sh | PROFILE_CAPS declaration | The task brief's KNOWN ROWS list called this table a clean DUPLICATE of PROFILE_CAPS (~1,598 B); measured and refuted -- a whitespace-normalised LCS of the whole table against config/profiles.sh returns 0 bytes (verified with the mandated python3 -c command), because the table transposes PROFILE_CAPS into per-capability rows while the source is keyed per-profile, so no literal string survives the transposition. Reclassified HAZARD: the table is original, non-duplicate content that happens to restate the same information a different way.
HAZARD | is gone — it belonged to a retired mac mini that no hostname has mapped to since, and `PROFILE_CAPS[server]` was deleted with it. | - | - | Prevents a reader from expecting a `server` profile key that no longer exists in PROFILE_MAP/PROFILE_CAPS; checked via LCS against config/profiles.sh (no >=20-byte overlap), so it is original content, not a duplicate. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
DUPLICATE-partial | returns it whenever the machine is o | config/profiles.sh | PROFILE_MAP header comment on -1 suffixes | Verified via LCS: 37-byte literal overlap ("returns it whenever the machine is o[ff/n]") with the source file's own comment on the -1 wireless-suffix convention. VERDICT: partial — the match is an LCS artifact that stops at the shared letter 'o' immediately before the two files diverge (CLAUDE.md: "off ethernet"; config/profiles.sh: "on wifi"). phrase_check.match_phrase finds 0 occurrences of the full CLAUDE.md sentence in profiles.sh. Nothing removed.
DUPLICATE-partial | take a single key because each | config/profiles.sh | PROFILE_MAP header comment on workstation/cruncher/claude | Verified via LCS: 74-byte literal overlap with the source comment explaining the single-DHCP-registration criterion; the match stops at 'each' because the source comment wraps to a new `#`-prefixed line before 'holds', which breaks literal continuation. VERDICT: partial — CLAUDE.md's markdown prose (bold formatting, "— that is the criterion, not the interface count and not the absence of wireless hardware") is not byte-identical to profiles.sh's plain comment ("holds ONE DHCP/DNS registration. That is the criterion -- not the interface count, and not the absence of wireless hardware"). phrase_check.match_phrase finds 0 occurrences of the full CLAUDE.md sentence in profiles.sh. Shares a paragraph with the home-1 row (line 330); disposed together, same verdict. Nothing removed.
DUPLICATE-partial | is the one exception to the suffix meaning "wireless": there the | config/profiles.sh | PROFILE_MAP header comment on home-1 | Verified via LCS: 75-byte literal overlap with the source comment on the home-1 naming exception. VERDICT: partial — CLAUDE.md's full sentence ("...and it has no separate `home` entry.") is not byte-identical to profiles.sh's comment ("...a naming mistake kept because a `home-2` may follow.", no "no separate home entry" clause). phrase_check.match_phrase finds 0 occurrences of the full CLAUDE.md sentence in profiles.sh. Shares a paragraph with the workstation/cruncher/claude row (line 329); disposed together, same verdict. Nothing removed.
HAZARD | that is a silent, well-formed answer, not an error, which is why `run_doctor` now checks for it explicitly | - | - | Non-obvious failure mode: an unmapped hostname resolves to PROFILE=unknown with zero HAS_* rather than erroring; checked via LCS against config/profiles.sh (no match), so original content.
AMBIGUOUS | it for: | - | - | Bare two-word list header (GitHub MCP's 'Use it for:'); trivial connector, not a substantive claim.
AMBIGUOUS | coding standards. Dotfiles-specific notes: | - | - | Pointer + connector sentence introducing the Shell Scripts bullet list; not itself a hazard/duplicate/record/reference claim.
HAZARD | chsh` is setuid root but authenticates the **invoking** user through PAM, so it prompts for a password and exits 1 in every non-interactive actor | - | - | Names the specific PASS-vs-FAIL bug this paragraph introduces: chsh silently fails under every non-interactive actor (provision run, cron, ssh).
AMBIGUOUS | sudo -n chsh -s /bin/zsh "$USER" | - | - | Illustrative terminal transcript showing the passwordless-sudo form succeeding where bare chsh fails; plain example, not a claim.
HAZARD | narrows the actors that see it from login shells to interactive ones | - | - | Explains why _OVERRIDE_DOCKER_BIN's block moved from .zprofile into the interactive-only file, and which actors lose the PATH entry as a result.
HAZARD | so a seam that only redirects `HOME` cannot force resolution to a fixture, because `command -v` runs first and wins on any machine that already has a real `ledger` on `PATH` | - | - | Core LEDGER_BIN hazard: resolution order puts the operator's PATH ahead of a HOME-only test redirect, so only a PATH-prepended mock (tests/mocks/ledger) closes the gap.
HAZARD | a fixture file named `lib/developer.sh` lands on the real allowlist key without needing a second seam for the allowlist itself | - | - | Explains the specific mechanics of _OVERRIDE_LIB_TRAP_SCOPE's fixture-root glob versus the real git ls-files scan.
AMBIGUOUS | one file per cadence, beside that agent's launchd logs: | - | - | Connector sentence introducing the heartbeat JSON code-fence example; not itself a claim.
AMBIGUOUS | properties are load-bearing and none is obvious from the shape: | - | - | Bare connector sentence introducing the four heartbeat-contract bullets (max_age_days/pending/held/findings); not itself a claim. Re-anchored: the previous phrase began its paragraph, which phrase_check now correctly treats as sentence-initial (a later remedy capitalises a leading word). New span is word-boundary-aligned, occurs once, and is not a paragraph prefix.
HAZARD | A test drives the timeout branch with a stub that sleeps 3s under | - | - | Describes how _DOCTOR_PROBE_TIMEOUT's own timeout override is exercised without waiting on the real 10-second bound.
AMBIGUOUS | that the earlier analysis got wrong or could not see: | - | - | Bare connector sentence introducing the mode=silent / pinDigests bullets; not itself a claim.
HAZARD | `Branch … creation is disabled because mode=silent` appears verbatim | - | - | Names the specific Mend job-log evidence that established (and later retired) the silent-mode finding; a stale finding must not be cited as a live constraint without re-reading a current job log.
AMBIGUOUS | mechanical details, both non-obvious: | - | - | Bare connector sentence introducing the automated-security-fixes bullets that follow; not itself a claim.
RECORD-deleted | and that was already wrong before this branch | - | - | Deleted: the full intro sentence ("This section has claimed a single-file edit since it was written...") was removed — the "Measured directly against `origin/master`..." paragraph that follows conveys the correction (5 files before, 3-4 after) on its own without the meta-framing. Removed from CLAUDE.md.
REFERENCE | add **both** the wired and the wireless-interface hostname to `PROFILE_MAP`, mapped to the same profile | - | - | Onboarding step 1; only exercised when adding a machine, which the already-provisioned fleet never does again.
REFERENCE | [my-new-host]="mac_workstation" [my-new-host-1]="mac_workstation" | - | - | Illustrative code-fence example for onboarding step 1; scaffolding, not a standing claim.
REFERENCE | add the identical wired/wireless pair to `PROFILE_LEGACY`, mapping both hostnames to the same legacy identity variable name | - | - | Onboarding step 2; same reference scope as step 1.
HAZARD | a hostname swapped onto the wrong legacy variable in `PROFILE_LEGACY` itself would agree with the very table it exists to check, and pass silently | - | - | Restates, for the onboarding step, the same same-decision-cannot-falsify argument given in the Test Seams legacy_oracle.bash paragraph -- an internal echo within this file rather than a cross-file duplicate, since both copies live in CLAUDE.md itself.
HAZARD | a missing `PROFILE_LEGACY` entry degrades gracefully | - | - | Non-obvious consequence table: what actually happens if either onboarding step is skipped and a bypassed hook lets it through anyway.
REFERENCE | add its key to the `wired_only` set in `tests/setup_env/profiles.bats` | - | - | Onboarding edge-case step for a wired-only host; reference scaffolding for the rare add-a-machine event.
REFERENCE | a feature branch — CI validates → auto-merges to master | - | - | Final onboarding step; plain workflow reference.
HAZARD | a wired-only host needs a third test edit besides | - | - | Clarifies the true edit count (production vs. test files) after the corrected step list, closing the gap the RECORD paragraph above named.
HAZARD | that is the check that catches the omission in step 1 rather than letting the machine silently run with no capabilities | - | - | Names run_doctor as the safety net for a hostname missing from the table, tying back to the Profile Model section's unmapped-hostname hazard.
HAZARD | read unconditionally inside the `HAS_DOCKER` block, not only under test | - | - | Explains why _OVERRIDE_DOCKER_BIN must be driven through the override rather than the real filesystem: the real Docker Desktop directory exists on any mac running it, so the absent-branch test would short-circuit the -d guard.
# Phrase manifest -- fragment D (gap-fill: the 10 content paragraphs left
# unclassified by fragments A, B, and C -- see docs/superpowers/plans/phrases-{a,b,c}.md)
#
# class | phrase | counterpart-file | counterpart-symbol | note

# ---- Language Standards ----

# ---- Testing (venv snapshot rollback) ----
HAZARD | "$(pyenv which python)" -m pip install --no-deps -r ~/.local/share/dotfiles/venv-snapshots/ansible-<UTC>.txt | - | - | The `--no-deps` flag is not stylistic -- the surrounding prose states the pre-sync venv state is provably unsatisfiable via ordinary pip dependency resolution (pip reached it incrementally; the cumulative set has no single consistent resolution). An operator restoring from a snapshot without this exact flag gets a resolver failure and may conclude the snapshot is corrupt rather than that the flag was dropped.

# ---- PowerShell Testing ----
REFERENCE | make test   # runs PSScriptAnalyzer lint then Pester tests | - | - | Mechanical command reference for running the PowerShell suite from the `powershell/` subdirectory; the recipe itself is discoverable by reading `powershell/Makefile`.
REFERENCE | Install-Module Pester -Force -Scope CurrentUser -MinimumVersion 5.0 | - | - | One-time prerequisite install command for the Pester/PSScriptAnalyzer toolchain; plain scaffolding, no hidden consequence beyond what running it shows.

# ---- Coverage (Bash) ----
REFERENCE | bash scripts/run-bash-coverage.sh --count-coverable lib/helpers.sh | - | - | Example CLI invocation for inspecting one file's coverage denominator without running the full bats suite; mechanical, discoverable by reading `scripts/run-bash-coverage.sh --help`-equivalent usage.

# ---- Key Conventions: "Which `make` an actor resolves" (5-paragraph argument; p215 is the
# measured evidence for the HAZARD claimed in p214, and both sit downstream of the RECORD/
# DUPLICATE pair at p209/p210 that establish the actor table itself. A deletion pass must
# keep p214+p215 together -- the HAZARD without its evidence is an unverified assertion. ----
RECORD-partial | sourced by interactive zsh only, `make`'s version on a provisioned mac is a function of how the process was started | - | - | Partial: shortened the table's intro sentence from "There are four answers, not the two this file used to describe, all measured 2026-08-16 on the Studio:" to "There are four answers:" — the table itself carries the current content; the self-correction framing and date were provenance only. Re-anchored: the shortened intro sentence removed the original phrase; re-pointed at the surviving clause earlier in the same one-line paragraph (CLAUDE.md, count 1, not sentence-initial).
DUPLICATE-judgement | `6_path.zsh` / `.zprofile` | ~/.claude/standards/behavior.md | "A boundary can be an actor, not a place" section, actor table, interactive-zsh row's PATH-source cell | CLAUDE.md (Key Conventions, "Which `make` an actor resolves" table, interactive-zsh row, PATH-source column): "`6_path.zsh` / `.zprofile`". behavior.md ("A boundary can be an actor, not a place" section, actor table, interactive-zsh row, PATH-source cell): "`6_path.zsh` / `.zprofile`". Measured: phrase_check.match_phrase(normalize(behavior.md), the full declared phrase) = 1 — unlike every other row in this task, this is a literal, byte-identical duplicate, confirmed via direct whitespace-normalised substring search. Shared: the exact table cell. Diverges: nothing in the cell itself — the surrounding tables differ (behavior.md collapses cron/launchd/ssh into one row and carries no separate `resolves`/`version` columns; CLAUDE.md's table is 4 columns by 4 actor rows specific to this repo's own measurement). VERDICT: judgement, not deletion — --deleted-have-counterparts would in fact pass here (count=1), but the phrase is one cell inside a markdown table row; removing only the cell's text without restructuring the row would corrupt the table (a dangling empty column), and this task's scope is manifest-only with no CLAUDE.md edits. Structural table repair (merging or dropping the column) is deferred to the compression plan. Nothing removed.
AMBIGUOUS | because both cost real work and neither is obvious: | - | - | Bare connector sentence introducing the two /usr/local/bin and PATH-shadowing traps that follow; matches the established house style for introductory sentences (fragment C's "Three properties are load-bearing and none is obvious from the shape:" and siblings) -- fits none of the four classes on its own.
HAZARD | a human at a prompt and not for anything else | - | - | Standing operational hazard, not merely historical: because `6_path.zsh` (which puts Homebrew's brew on PATH) is sourced by interactive zsh only, `setup_env.sh`'s `env which brew` prerequisite check means no cron job, git hook, CI runner, or non-interactive agent session can run this entry point on the Linux workstation at all -- it dies in seconds with advice ("run bootstrap_linux.sh") that is actively wrong for a machine already bootstrapped. Checked against behavior.md and shell.md for the same claim about this specific script; not found -- this is dotfiles-specific narrative, not a cross-file duplicate.
HAZARD | non-interactive bash : ABSENT | - | - | This is the measured evidence for the HAZARD immediately above (p214): the two-line probe output proving the actor split is real on this exact machine, not asserted. Deleting this block without the claim it supports turns a measured finding back into an unverified assertion; deleting the claim without this block strands a bare command transcript with no stated consequence. The two rows must be treated as one unit in any later pruning pass.

# EXEMPT p51 `@~/.claude/standards/powershell.md` — no row is possible. The bare path
# occurs 3 times in CLAUDE.md, so uniqueness requires the leading `@`; the `@` sits
# immediately after a sentence-ending period, so every unique candidate is
# sentence-initial and the tool rejects it. Exhaustive search under the tool's own
# match_phrase predicate confirms 2 such paragraphs in 249: this and `CI requirements:`.
HAZARD | is **exhaustive at the top level** — every tracked top-level directory appears — and illustrative within a directory except `lib/`, which is complete. | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.
REFERENCE | (emit per-task YAML `yaml-task` blocks with `role`/`model`/`tdd`/`acceptance`/`max_retries`/`files_touched`/`depends_on`/`parallel_group`). Opus role. | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.
REFERENCE | repo lives in `ai-config/docs/knowledge/` under `dotfiles-<topic>.md` naming (per ADR-0020). The local `docs/knowledge/README.md` is a pointer stub. | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.
HAZARD | where code branches by OS — for example `lib/detect_env.sh:80` and `.config/.zshrc.d/5_general.zsh:69`. `setup_env.sh` itself contains no such branch, | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.
REFERENCE | lib/constants.sh for current pins: GO_VER, PYTHON_VER, RUBY_VER, | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.
HAZARD | loaded by `.config/.zshrc.d/5_general.zsh`). Not interchangeable across platforms. Handled automatically by `install_ruby()`/`install_ruby_tools()` | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.
REFERENCE | jobs run on `ubuntu-latest` with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.
HAZARD | only the two Homebrew prefixes, after `gh` and `python3`. `ledger_drift_check.sh` resolves the binary with a bare `command -v ledger`, returns **1** | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate. Re-anchored during pr-review final: the previous span was inside the sentence whose unanchored past tense contradicted the plist PATH printed two lines above it, so correcting that CLAUDE.md sentence consumed the anchor.
RECORD | consolidation, a new machine's legacy identity variable needed a hardcoded case arm in **five** separate files — `config/profiles.sh` (`PROFILE_MAP`), | - | - | Added during pr-review cycle 1 to close the --assert-complete-derived coverage gap. This paragraph's original row was consumed when its phrase was deleted or re-anchored, leaving the paragraph unclassified and therefore invisible to every later gate.

HAZARD | — wiring it would commit every future CLAUDE.md edit to keeping every phrase anchor valid, with no owner — but it is deliberate rather than decided: | - | - | Added by the post-merge docs audit for the paragraph it introduced documenting that phrase_check's suite is gated and the tool is not. HAZARD because the trap it names is reading suite coverage as evidence the manifest is enforced.
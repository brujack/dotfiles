# Superpowers Specs and Plans

Master status index for all specs and implementation plans in this directory.

## Status Key

| Status      | Meaning                                                                 |
| ----------- | ----------------------------------------------------------------------- |
| Done        | Implemented and merged to master                                        |
| In Progress | Currently being implemented                                             |
| Pending     | Not yet started                                                         |
| Retired     | Design falsified before implementation; kept for the record, superseded |

---

## All Plans

| Date       | Plan                                                                                       | Spec                                                                                                                                      | Status      |
| ---------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2026-08-27 | [ci-job-timeouts](plans/2026-08-27-ci-job-timeouts.md) | [spec](specs/2026-08-27-ci-job-timeouts-design.md) | Done |
| 2026-08-23 | [identity-table-fail-closed](plans/2026-08-23-identity-table-fail-closed.md) | [spec](specs/2026-08-23-profiles-bash-version-guard-design.md) | Done |
| 2026-08-20 | —                                                                                          | [spec](specs/2026-08-20-setup-env-brew-probe-design.md)                                                                                   | Closed |
| 2026-08-20 | —                                                                                          | [spec](specs/2026-08-20-zshenv-brew-reachability-design.md)                                                                               | Blocked |
| 2026-08-20 | —                                                                                          | [spec](specs/2026-08-20-linuxbrew-login-path-design.md)                                                                                   | Retired |
| 2026-08-20 | [brewfile-uv](plans/2026-08-20-brewfile-uv.md)                                             | [spec](specs/2026-08-20-brewfile-uv-design.md)                                                                                            | Done        |
| 2026-08-21 | —                                                                                          | [spec](specs/2026-08-21-venv-manifest-corrections-design.md)                                                                                | Done        |
| 2026-03-27 | [bats-testing](plans/2026-03-27-bats-testing.md)                                           | [spec](specs/2026-03-27-bats-testing-design.md)                                                                                           | Done        |
| 2026-03-27 | [test-coverage-expansion](plans/2026-03-27-test-coverage-expansion.md)                     | —                                                                                                                                         | Done        |
| 2026-03-28 | [makefile-lint](plans/2026-03-28-makefile-lint.md)                                         | [spec](specs/2026-03-28-makefile-lint-design.md)                                                                                          | Done        |
| 2026-03-28 | [powershell-tests](plans/2026-03-28-powershell-tests.md)                                   | [spec](specs/2026-03-28-powershell-tests-design.md)                                                                                       | Done        |
| 2026-03-28 | [powershell-setup-improvements](plans/2026-03-28-powershell-setup-improvements.md)         | [spec](specs/2026-03-28-powershell-setup-improvements-design.md)                                                                          | Done        |
| 2026-03-28 | [setup-env-function-extraction](plans/2026-03-28-setup-env-function-extraction.md)         | [spec](specs/2026-03-28-setup-env-function-extraction-design.md)                                                                          | Done        |
| 2026-03-28 | [test-coverage-remaining](plans/2026-03-28-test-coverage-remaining.md)                     | [spec](specs/2026-03-28-test-coverage-remaining-design.md)                                                                                | Done        |
| 2026-03-28 | [zshrc-d-test-coverage](plans/2026-03-28-zshrc-d-test-coverage.md)                         | [spec](specs/2026-03-28-zshrc-d-test-coverage-design.md)                                                                                  | Done        |
| 2026-03-31 | [dotfiles-phase0-bootstrap](plans/2026-03-31-dotfiles-phase0-bootstrap.md)                 | [spec](specs/2026-03-31-dotfiles-modularization-design.md)                                                                                | Done        |
| 2026-03-31 | [dotfiles-phase1-lib-split](plans/2026-03-31-dotfiles-phase1-lib-split.md)                 | [spec](specs/2026-03-31-dotfiles-modularization-design.md)                                                                                | Done        |
| 2026-03-31 | [dotfiles-phase2-hardening](plans/2026-03-31-dotfiles-phase2-hardening.md)                 | [spec](specs/2026-03-31-dotfiles-modularization-design.md)                                                                                | Done        |
| 2026-03-31 | [dotfiles-phase3-profiles](plans/2026-03-31-dotfiles-phase3-profiles.md)                   | [spec](specs/2026-03-31-dotfiles-modularization-design.md)                                                                                | Done        |
| 2026-03-31 | [dotfiles-phase4-ci](plans/2026-03-31-dotfiles-phase4-ci.md)                               | [spec](specs/2026-03-31-dotfiles-modularization-design.md)                                                                                | Done        |
| 2026-03-31 | [dotfiles-phase5-docs](plans/2026-03-31-dotfiles-phase5-docs.md)                           | [spec](specs/2026-03-31-dotfiles-modularization-design.md)                                                                                | Done        |
| 2026-04-02 | [brewfile-profile-split](plans/2026-04-02-brewfile-profile-split.md)                       | [spec](specs/2026-04-02-brewfile-profile-split-design.md)                                                                                 | Done        |
| 2026-04-05 | [mas-brewfile-integration](plans/2026-04-05-mas-brewfile-integration.md)                   | [spec](specs/2026-04-05-mas-brewfile-integration-design.md)                                                                               | Done        |
| 2026-04-07 | [macos-capability-migration](plans/2026-04-07-macos-capability-migration.md)               | [spec](specs/2026-04-05-macos-setup-capability-migration-design.md)                                                                       | Done        |
| 2026-04-07 | [linux-capability-migration](plans/2026-04-07-linux-capability-migration.md)               | [spec](specs/2026-04-05-linux-setup-capability-migration-design.md)                                                                       | Done        |
| 2026-04-08 | [secrets-guardrails](plans/2026-04-08-secrets-guardrails.md)                               | [spec](specs/2026-04-08-secrets-guardrails-design.md)                                                                                     | Done        |
| 2026-04-08 | [workflows-extraction](plans/2026-04-08-workflows-extraction.md)                           | [spec](specs/2026-04-08-workflows-extraction-design.md)                                                                                   | Done        |
| 2026-04-08 | [doctor-dry-run](plans/2026-04-08-doctor-dry-run.md)                                       | [spec](specs/2026-04-08-doctor-dry-run-design.md)                                                                                         | Done        |
| 2026-04-08 | [ci-safety](plans/2026-04-08-ci-safety.md)                                                 | [spec](specs/2026-04-08-ci-safety-design.md)                                                                                              | Done        |
| 2026-04-08 | [plan-hygiene](plans/2026-04-08-plan-hygiene.md)                                           | [spec](specs/2026-04-08-plan-hygiene-design.md)                                                                                           | Done        |
| 2026-04-08 | [local-overrides](plans/2026-04-08-local-overrides.md)                                     | [spec](specs/2026-04-08-local-overrides-design.md)                                                                                        | Done        |
| 2026-04-08 | [granular-update-flags](plans/2026-04-08-granular-update-flags.md)                         | [spec](specs/2026-04-08-granular-update-flags-design.md)                                                                                  | Done        |
| 2026-04-08 | [doctor-enhanced](plans/2026-04-08-doctor-enhanced.md)                                     | [spec](specs/2026-04-08-doctor-enhanced-design.md)                                                                                        | Done        |
| 2026-04-08 | [workflow-test-coverage](plans/2026-04-08-workflow-test-coverage.md)                       | [spec](specs/2026-04-08-workflow-test-coverage-design.md)                                                                                 | Done        |
| 2026-04-08 | [check-versions](plans/2026-04-08-check-versions.md)                                       | [spec](specs/2026-04-08-check-versions-design.md)                                                                                         | Done        |
| 2026-04-09 | [adr-implementation](plans/2026-04-09-adr-implementation.md)                               | [spec](specs/2026-04-09-adr-design.md)                                                                                                    | Done        |
| 2026-04-10 | [brew-mas-install-flags](plans/2026-04-10-brew-mas-install-flags.md)                       | [spec](specs/2026-04-10-brew-mas-install-flags-design.md)                                                                                 | Done        |
| 2026-04-10 | [cursor-sync](plans/2026-04-10-cursor-sync.md)                                             | [spec](specs/2026-04-10-cursor-sync-design.md)                                                                                            | Done        |
| 2026-04-10 | [logic-review-process](plans/2026-04-10-logic-review-process.md)                           | [spec](specs/2026-04-10-logic-review-process-design.md)                                                                                   | Done        |
| 2026-04-10 | [workflows-refactor](plans/2026-04-10-workflows-refactor.md)                               | [spec](specs/2026-04-10-workflows-refactor-design.md)                                                                                     | Done        |
| 2026-04-10 | [interactive-version-update](plans/2026-04-10-interactive-version-update.md)               | [spec](specs/2026-04-10-interactive-version-update-design.md)                                                                             | Done        |
| 2026-04-11 | [update-summary](plans/2026-04-11-update-summary.md)                                       | [spec](specs/2026-04-11-update-summary-design.md)                                                                                         | Done        |
| 2026-04-11 | [bootstrap-tests](plans/2026-04-11-bootstrap-tests.md)                                     | [spec](specs/2026-04-11-bootstrap-tests-design.md)                                                                                        | Done        |
| 2026-04-14 | [pr-review-gate](plans/2026-04-14-pr-review-gate.md)                                       | [spec](specs/2026-04-14-pr-review-gate-design.md)                                                                                         | Done        |
| 2026-04-14 | [github-mcp](plans/2026-04-14-github-mcp.md)                                               | [spec](specs/2026-04-14-github-mcp-design.md)                                                                                             | Done        |
| 2026-04-15 | [refactor-skill](plans/2026-04-15-refactor-skill.md)                                       | [spec](specs/2026-04-15-refactor-skill-design.md)                                                                                         | Done        |
| 2026-04-16 | [linux-package-update-tracking](plans/2026-04-16-linux-package-update-tracking.md)         | [spec](specs/2026-04-16-linux-package-update-tracking-design.md)                                                                          | Done        |
| 2026-04-21 | [dual-mode-ci-dotfiles-math](plans/2026-04-21-dual-mode-ci-dotfiles-math.md)               | [spec](specs/2026-04-21-dual-mode-ci-dotfiles-math-design.md)                                                                             | Done        |
| 2026-04-26 | [powershell-coverage-gate](plans/2026-04-26-powershell-coverage-gate.md)                   | [spec](specs/2026-04-26-powershell-coverage-gate-design.md)                                                                               | Done        |
| 2026-04-26 | [bash-coverage-gate](plans/2026-04-26-bash-coverage-gate.md)                               | [spec](specs/2026-04-26-bash-coverage-gate-design.md)                                                                                     | Done        |
| 2026-04-28 | [linux-sh-split](plans/2026-04-28-linux-sh-split.md)                                       | [spec](specs/2026-04-28-linux-sh-split-design.md)                                                                                         | Done        |
| 2026-04-28 | [brewfile-drift](plans/2026-04-28-brewfile-drift.md)                                       | [spec](specs/2026-04-28-brewfile-drift-design.md)                                                                                         | Done        |
| 2026-04-29 | [brewfile-drift-leaves](plans/2026-04-29-brewfile-drift-leaves.md)                         | [spec](specs/2026-04-29-brewfile-drift-leaves-design.md)                                                                                  | Done        |
| 2026-05-01 | [linux-ubuntu-dedup](plans/2026-05-01-linux-ubuntu-dedup.md)                               | [spec](specs/2026-05-01-linux-ubuntu-dedup-design.md)                                                                                     | Done        |
| 2026-05-08 | [whats-new-anthropic](plans/2026-05-08-whats-new-anthropic.md)                             | [spec](specs/2026-05-08-whats-new-anthropic-design.md)                                                                                    | Done        |
| 2026-05-13 | [ai-config-split](plans/2026-05-13-ai-config-split.md)                                     | [spec](specs/2026-05-13-ai-config-split-design.md)                                                                                        | Done        |
| 2026-05-14 | [devcontainer-removal](plans/2026-05-14-devcontainer-removal.md)                           | [spec](specs/2026-05-14-devcontainer-removal-design.md)                                                                                   | Done        |
| 2026-05-18 | [windows-ai-native-setup](plans/2026-05-18-windows-ai-native-setup.md)                     | [spec](specs/2026-05-18-windows-ai-native-setup-design.md)                                                                                | Done        |
| 2026-05-27 | [coverage-brewfile-helpers](plans/2026-05-27-coverage-brewfile-helpers.md)                 | —                                                                                                                                         | Done        |
| 2026-05-27 | coverage-run-update-optional-tools                                                         | —                                                                                                                                         | Done        |
| 2026-05-27 | coverage-run-update-sections                                                               | —                                                                                                                                         | Done        |
| 2026-05-27 | coverage-install-terraform-skill                                                           | —                                                                                                                                         | Done        |
| 2026-05-27 | coverage-setup-env-direct-run                                                              | —                                                                                                                                         | Done        |
| 2026-05-28 | coverage-setup-env-preamble                                                                | [spec](specs/2026-05-28-coverage-setup-env-preamble-design.md)                                                                            | Done        |
| 2026-05-28 | coverage-developer-gaps                                                                    | [spec](specs/2026-05-28-coverage-developer-gaps-design.md)                                                                                | Done        |
| 2026-05-28 | coverage-helpers-gaps                                                                      | —                                                                                                                                         | Done        |
| 2026-05-28 | coverage-workflows-update-flags                                                            | —                                                                                                                                         | Done        |
| 2026-05-28 | coverage-workflows-setup-chains                                                            | —                                                                                                                                         | Done        |
| 2026-05-28 | coverage-update-summary-gaps                                                               | —                                                                                                                                         | Done        |
| 2026-05-28 | coverage-workflows-gaps                                                                    | —                                                                                                                                         | Done        |
| 2026-05-29 | [coverage-update-summary-casks](plans/2026-05-29-coverage-update-summary-casks.md)         | [spec](specs/2026-05-29-coverage-update-summary-casks-design.md)                                                                          | Done        |
| 2026-05-29 | [coverage-helpers-gaps-2](plans/2026-05-29-coverage-helpers-gaps-2.md)                     | [spec](specs/2026-05-29-coverage-helpers-gaps-2-design.md)                                                                                | Done        |
| 2026-05-30 | [coverage-per-file-gaps](plans/2026-05-30-coverage-per-file-gaps.md)                       | —                                                                                                                                         | Done        |
| 2026-05-31 | coverage-macos-install-errors                                                              | —                                                                                                                                         | Done        |
| 2026-05-31 | coverage-helpers-doctor-error-paths                                                        | —                                                                                                                                         | Done        |
| 2026-05-31 | coverage-helpers-setup-functions                                                           | —                                                                                                                                         | Done        |
| 2026-05-31 | coverage-workflows-pip-update                                                              | —                                                                                                                                         | Done        |
| 2026-06-01 | coverage-workflows-minor-paths                                                             | —                                                                                                                                         | Done        |
| 2026-06-01 | coverage-more-tests                                                                        | —                                                                                                                                         | Done        |
| 2026-06-01 | coverage-gap-tests                                                                         | —                                                                                                                                         | Done        |
| 2026-06-09 | [recreate-venv](plans/2026-06-09-recreate-venv.md)                                         | [spec](specs/2026-06-09-recreate-venv-design.md)                                                                                          | Done        |
| 2026-06-17 | [ubuntu-2604-pr1](plans/2026-06-17-ubuntu-2604-pr1.md)                                     | [spec](specs/2026-06-17-ubuntu-2604-pr1-design.md)                                                                                        | Done        |
| 2026-06-17 | [ubuntu-2604-pr2](plans/2026-06-17-ubuntu-2604-pr2.md)                                     | [spec](specs/2026-06-17-ubuntu-2604-pr2-design.md)                                                                                        | Done        |
| 2026-06-17 | [ubuntu-2604-pr3](plans/2026-06-17-ubuntu-2604-pr3.md)                                     | [spec](specs/2026-06-17-ubuntu-2604-pr3-design.md)                                                                                        | Done        |
| 2026-06-17 | [ubuntu-2604-pr4](plans/2026-06-17-ubuntu-2604-pr4.md)                                     | [spec](specs/2026-06-17-ubuntu-2604-pr4-design.md)                                                                                        | Done        |
| 2026-06-17 | [ubuntu-2604-pr5](plans/2026-06-17-ubuntu-2604-pr5.md)                                     | [spec](specs/2026-06-17-ubuntu-2604-pr5-design.md)                                                                                        | Done        |
| 2026-06-18 | ubuntu-2604-p3 cleanup (PR #149)                                                           | [spec](specs/2026-06-17-ubuntu-2604-support-design.md)                                                                                    | Done        |
| 2026-06-19 | [pip-venv-audit](plans/2026-06-19-pip-venv-audit.md)                                       | [spec](specs/2026-06-19-pip-venv-audit-design.md)                                                                                         | Done        |
| 2026-06-21 | [secure-curl-installs](plans/2026-06-21-secure-curl-installs.md)                           | [spec](specs/2026-06-21-secure-curl-installs-design.md)                                                                                   | Done        |
| 2026-06-28 | state-ledger-integration (PR dotfiles#166, #167)                                           | [spec](specs/2026-06-27-state-ledger-design.md)                                                                                           | Done        |
| 2026-06-28 | state-ledger all run_* wiring, _DOTFILES_RUN_TMPDIR rename (PR dotfiles#168)               | —                                                                                                                                         | Done        |
| 2026-07-04 | [recreate-ruby](plans/2026-07-04-recreate-ruby.md) (PR dotfiles#172, #173)                 | [spec](specs/2026-07-04-recreate-ruby-design.md)                                                                                          | Done        |
| 2026-07-05 | [recreate-ruby-gem-update](plans/2026-07-05-recreate-ruby-gem-update.md) (PR dotfiles#174) | [spec](specs/2026-07-05-recreate-ruby-gem-update-design.md)                                                                               | Done        |
| 2026-07-07 | [state-ledger-bootstrap](plans/2026-07-07-state-ledger-bootstrap.md) (PR dotfiles#176)     | [spec](specs/2026-07-07-state-ledger-bootstrap-design.md)                                                                                 | Done        |
| 2026-07-18 | [sync-git-repos](plans/2026-07-18-sync-git-repos.md) (PR dotfiles#182)                     | [spec](specs/2026-07-18-sync-git-repos-design.md)                                                                                         | Done        |
| 2026-07-19 | scripts-dir-cleanup (PR dotfiles#183)                                                      | [spec](specs/2026-07-19-scripts-dir-cleanup-design.md)                                                                                    | Done        |
| 2026-07-19 | scripts-world-class (PR dotfiles#185)                                                      | [spec](specs/2026-07-19-scripts-world-class-design.md)                                                                                    | Done        |
| 2026-07-28 | [auto-install-git-hooks](plans/2026-07-28-auto-install-git-hooks.md)                       | [spec](specs/2026-07-28-auto-install-git-hooks-design.md)                                                                                 | Done        |
| 2026-07-29 | [hooks-sweep-gitdir-strip](plans/2026-07-29-hooks-sweep-gitdir-strip.md) (PR dotfiles#190) | [spec](specs/2026-07-29-doctor-hookspath-check-design.md)                                                                                 | Done        |
| 2026-07-29 | [hookspath-global-system-detector](plans/2026-07-29-hookspath-global-system-detector.md)   | [spec](specs/2026-07-29-doctor-hookspath-check-design.md)                                                                                 | Done        |
| 2026-07-29 | [npm-global-pinning](plans/2026-07-29-npm-global-pinning.md) (PR dotfiles#192)             | [spec §C](https://github.com/brujack/ai-config/blob/master/docs/superpowers/specs/2026-07-29-maintainability-coverage-manifest-design.md) | Done        |
| 2026-07-30 | [test-git-env-hygiene](plans/2026-07-30-test-git-env-hygiene.md)                           | [spec](specs/2026-07-29-test-git-env-hygiene-design.md)                                                                                   | Done        |
| 2026-07-31 | [pre-push-self-coverage](plans/2026-07-31-pre-push-self-coverage.md) (PR dotfiles#195)     | [spec](specs/2026-07-31-git-hook-detection-gaps-design.md) + [ADR-0017](../adr/0017-pre-push-trigger-fail-closed.md)                      | Done        |
| 2026-08-11 | [bats-provisioning-zsh-lint-scope](plans/2026-08-11-bats-provisioning-zsh-lint-scope.md)   | [spec](specs/2026-08-11-bats-provisioning-zsh-lint-scope-design.md)                                                                       | Done        |
| 2026-08-12 | [gnu-make-4-on-macos](plans/2026-08-12-gnu-make-4-on-macos.md)                             | [spec](specs/2026-08-12-gnu-make-4-on-macos-design.md)                                                                                    | Done        |
| 2026-08-14 | [silent-false-success-cluster](plans/2026-08-14-silent-false-success-cluster.md)           | [spec](specs/2026-08-13-silent-false-success-cluster-design.md)                                                                           | Done        |
| 2026-08-15 | [shell-lint-shebang-scope](plans/2026-08-15-shell-lint-shebang-scope.md)                   | [spec](specs/2026-08-15-shell-lint-shebang-scope-design.md)                                                                               | Done        |
| 2026-08-16 | system-wide-gnu-make                                                                       | [spec](specs/2026-08-16-system-wide-gnu-make-design.md)                                                                                   | Retired     |
| 2026-08-16 | hook-make-resolution                                                                       | [spec](specs/2026-08-16-hook-make-resolution-design.md)                                                                                   | Retired     |
| 2026-08-16 | [zsh-identity-single-table](plans/2026-08-16-zsh-identity-single-table.md)                 | [spec](specs/2026-08-16-zsh-identity-single-table-design.md)                                                                              | Done        |
| 2026-08-17 | [zsh-legacy-identity-consolidation](plans/2026-08-17-zsh-legacy-identity-consolidation.md)                                                          | [spec](specs/2026-08-17-zsh-legacy-identity-consolidation-design.md)                                                                      | Done |

---

## Backlog

Ideas approved for future specs, in no particular order:

| Feature                                                                                 | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 7 orphaned worktree directories hold 192 permission rules the main checkout lacks | Found 2026-08-28 by the Step 13 leftover check during dotfiles#248 cleanup. `.claude/worktrees/` holds 7 directories dated 2026-05-27 to 2026-07-06 (`coverage+run-update-optional-tools`, `coverage+update-summary-casks`, `coverage-helpers-doctor-error-paths`, `coverage-helpers-setup-functions`, `recreate-ruby`, `remove-p10k`, `ubuntu-2604-pr4`), each containing exactly one file: `.claude/settings.local.json`. Invisible to both `git worktree list` and `git worktree prune`, so nothing will ever report them again. That is the debris signature `git-workflow.md` names, and the file-only-debris test passes — no plans or specs are at risk. **The reason this is a backlog row and not a `rm -rf`:** those 7 files carry **192 distinct allow rules the main checkout's 82 do not have** (per-orphan: 62/108/70/76/6/67/123 new), and they are the only copy. `git-workflow.md` records a Studio incident where 4 such orphans held 76 rules of which 12 were worth promoting and 3 were deliberately not — broad wildcards over credential and network tooling (`curl *`, `gh auth *`, `gh release *`), which `gh auth token` and `gh auth logout` make a real hazard rather than a convenience. So the work is a triage, not a delete: read the 192, promote the reusable ones, refuse the wildcards, then remove the directories. Promoting any of them is a permissions edit and needs the operator, not an agent acting on its own inference. |
| `cadence-notify.sh` discards the detector's stderr, and its finding count cannot express zero | Two findings in one file, surfaced 2026-08-28 while fixing the plist PATH and deliberately not fixed there — the operator approved the plist only. `:167` captures stdout with `2>/dev/null`, so a detector's diagnosis is thrown away: `ledger_drift_check.sh` printing `ERROR: ledger binary not found` to stderr left the operator with a notification naming no cause at all. `:170-171` sets `_count` by `grep -c .` over stdout, so *examined nothing* and *found one thing* are both `1` — the same collapse as the exit code, one layer up, and independent of it: it survives ai-config's 0/1/2 port. The two interact, which is why they belong in one change rather than two — capturing stderr into `_out` would inflate `_count` unless the streams are kept separate. A third wart falls out of the same lines: the `incomplete` arm labels `_out` as `Found so far:`, so anything a detector prints on the cannot-determine path renders as a finding and is counted as one. ai-config worked around all three by printing nothing to stdout on that path (measured: rc=2, empty stdout, `_count` 0), which is correct for them and leaves the wrapper wrong. **The over-count is no longer hypothetical.** Measured 2026-08-28 on the first genuine run of the ledger-drift agent, after `setup_user` re-installed the fixed plist: `ledger drift` found **31** stale entities and the heartbeat recorded **`"findings": 32`**, because `Stale entities found — alerting via ntfy` is a stdout line and `grep -c .` counts it. Off by one in the over-reporting direction, on real data, every week. The row previously argued from the zero-versus-one collapse alone; this is the same defect at the other end of the range and is the easier one to demonstrate. |
| 6 Dependabot alerts on transitive deps that no upgrade fixes | Measured 2026-08-27. `asteval` (2 medium, fixed in 1.0.9) is pinned `asteval==1.0.6` by `checkov` in every release 3.3.2-3.3.15, so no checkov bump clears it; `ecdsa` (1 high, GHSA-wj6h-64fc-37mp) reports `vulnerable >= 0` with no published patch. Both reach the repo only via `checkov`, and both manifests (`uv.lock`, `requirements-runtime-ci.txt`) are generated. Renovate cannot act: `pep621` reads `pyproject.toml`, where neither package is declared. Revisit when checkov relaxes the pin. Also explains dotfiles#227 - forcing asteval to 1.0.9 is unsatisfiable against that pin, so the resolver's only route was to walk checkov back a year. |
| a workflow-job `timeout-minutes` guard needs a YAML parser or `actionlint`, not `awk` | Specified and dropped across 3 review rounds; reasons, the 3 parser shapes it missed, and two viable futures are in [spec](specs/2026-08-27-ci-job-timeouts-design.md) under "What was considered and dropped". |
| `brew_install_cask` has no production caller | Found by `bug-scan` Step 3b's unwired sweep, 2026-08-26. Defined in `lib/helpers.sh`; the only other occurrence fleet-wide is a 2026-03-27 plan document describing a test for it. No production call site and no live test. Decide: delete it, or wire it where `brew bundle` currently handles casks. |
| `ledger_flush_spool` has no production caller, and an ADR asserts it works | Found by the same sweep. Defined in `lib/workflows.sh`; the only other occurrences are in `docs/adr/0014-state-ledger-cmdb-integration.md`, which states the spool mechanism "handles offline scenarios (no network)" — an ADR asserting a behaviour nothing invokes. That is the sharper half: the spool is the ledger's offline-durability story and the flush half of it never runs, so anything spooled stays spooled. Decide whether the ADR's claim or the code is what changes. |
| the cadence heartbeat has no reader that actually runs — **operator chose a `SessionStart` hook 2026-08-26; ai-config session owns the build** | Raised by the operator's architectural reviewer via the ai-config session, verified here and worse than reported. ADR-0024 splits signal (ntfy) from liveness (a heartbeat file read by `doctor`) precisely so a silent agent is distinguishable from a dead one. But **`doctor` is hand-invoked and effectively never runs**: `-t doctor` appears **1** time in 10,210 lines of this machine's shell history, and **nothing automated calls `run_doctor`** — the sole production call site is `setup_env.sh:72`, gated on the flag; no cron, no hook, no CI. So the heartbeat is correct code with no consumer, which is the exact `ledger_drift_check.sh` property the design was written to escape: a mechanism deferring to a reader that does not exist. Candidate readers, none obviously right: the two agents watching **each other's** heartbeat (self-contained, weekly detection, but blind if both die); a check in `run_update` (2 invocations in the same history — barely better); a `.zshrc.d` stat on interactive shell start (best coverage, but adds startup work and lands in the one file class shellcheck cannot lint); or a `SessionStart` hook (ai-config's surface, not this repo's). Needs its own decision, not a rushed one. |
| `lib/launch_agents.sh` carries three function prefixes | Raised by `pr-review` on the cadence branch and deliberately not fixed there. The file has `_rhn_` (5 functions), `_renovate_cadence_` (2) and `_la_` (1). `_rhn_` is a leftover from when the file was `renovate_cadence.sh` and the whole thing was renovate-specific; it now prefixes genuinely generic helpers — `_rhn_agent_dir`, `_rhn_launchctl`, `_rhn_is_cadence_host` — in a file that hosts two unrelated cadences, so a cold reader asks why the shared host check is renovate-branded. Not fixed in that branch because the rename spans production, three bats suites and seven documented `_RHN_*` seam names in `CLAUDE.md`, which is churn disproportionate to a readability finding on a branch that had already taken eight defect fixes. The decision to take: rename to a neutral prefix (`_cad_`?) including the env seams, or keep `_RHN_` for the seams as a stable public-ish contract and rename only the internal functions. |
| Under concurrent load the bats suite loses tests from its own PLAN, not just its results | Measured 2026-08-23 at `59e878f`, four runs at effectively the same tree. Uncontended: `1..1478`, 1478 ok, rc=0, three times. Once, while an `ai-config` suite (3441 tests) ran concurrently on the same box and files under `~/.claude` — a symlink into ai-config, read by 4 of this repo's test files — were being edited mid-run: **`1..1464`**, 1445 ok, 2 not ok, `Executed 1447 instead of expected 1464`. Two distinct symptoms, and the first is the unexplained one. The failures (`HAS_DEVTOOLS`/`HAS_DOCKER is unset for mac_mini`, `tests/setup_env/profiles.bats`) are environment leakage into the absent-capability tests Task 0 hardened, and both pass in isolation at the same SHA on a clean tree. But **bats discovered 14 fewer tests than the tree contains** — a plan shrink is not slowness, and nothing in `USER.md`'s suite-contention entry accounts for it. 17 further tests then died emitting no TAP line at all, which is the silent-loss class [[dotfiles-bats-exit-trap-clobbers-tap]] describes for a different cause. Worth chasing because "passes on re-run" is exactly how this class gets normalised: a run that loses tests from the plan reports a smaller green suite, and the only tell is comparing the plan against a known-good run. Three candidate mechanisms are already refuted, which narrows it usefully: (a) **not generated tests** — `grep -rln 'for .*@test|eval.*@test|bats_test_function' tests/` returns nothing, every `@test` here is a static literal, so discovery cannot depend on any input; (b) **not one skipped file** — no `.bats` file in the tree holds exactly 14 tests; (c) **not a stale file list** — `make test` runs `bats --recursive tests/`, a filesystem walk, not the `git ls-files`-derived `BATS_FILES` used by lint. The tree holds exactly 1478 literal `@test` blocks, matching a healthy plan. The peer session confirmed its own side is clean across 15 runs spanning the incident, all `1..486`, including one it was SIGKILLed mid-run — so a plan is written at discovery and survives the run dying, meaning a truncated run and a shrunken plan are distinguishable. What remains is a write-window during discovery, and nothing was writing under `tests/` at the time, so that is not yet explained either. Cheapest next step is to reproduce deliberately — run the two suites concurrently and diff the two plan lines — rather than waiting for it to recur. Found while re-running the gate for this PR, not by the gate. |
| The abort-vs-warn rationale is now stated in two files with nothing forcing them consistent | `lib/detect_env.sh:63-72` explains why the bash side aborts while `config/profiles.zsh:36-40` explains why the zsh side does not, in near-identical prose, and the bash comment cites the other by hardcoded line number (`profiles.zsh:41`). Both are correct today. Neither is derived from the other, so a future decision to change either side leaves the opposite file asserting something false, and a line inserted above `profiles.zsh:41` silently repoints the citation — `behavior.md`'s displacement class, where each reference is correct about its own subject and silent about drift between them. Deliberately not fixed: deleting either copy removes the warning from the site where someone would try to "reconcile the drift", which is the whole reason the comment exists, and asserting the disagreement in a test is more mechanism than the exposure earns. Also noted, not raised separately: `detect_env` is named as a query and mutates ~15 globals, and this branch made its return value load-bearing for the first time — a command-query split would touch every call site across bash, zsh and the suite for no behavioural gain. Found by `maintainability-review`'s lens pass (mechanical surface empty on a shell-only diff). |
| An ad-hoc reproduction script does not inherit the test harness's isolation, and nothing warns you | Measured 2026-08-23 during Task 2. An implementer reproducing a pre-fix bug wrote a throwaway `/tmp` debug script and ran the **real** `setup_env.sh -t update` against the **real** `$HOME` — the one invocation `CLAUDE.md` says must never happen unmocked, because `sync_git_repos.sh` performs real `git push` and `rsync --delete` over SSH and `_is_legacy_sync_host` triggers on the machine's actual `hostname -s`. It completed: `~/.dotfiles-update.log` carries a full `=== Update Summary — 2026-08-23 18:12:39 ===`, 22 sections, 16 OK. **No damage, and the reasons are worth knowing because none of them is discipline.** `legacy-rsync` reported `SKIP not studio` because a mocked `hostname` was still on `PATH`; `pip` reported `no changes` so the ansible venv was untouched despite a snapshot being written; and `sync_git_repos` never force-pushes by design, so the 18 local-only commits on the feature branch stayed local and `master` did not move — all verified independently rather than taken from the implementer's report. The transferable point, which the implementer named itself unprompted: **the bats suite's `setup()` is what supplies `HOME` isolation, and a hand-written reproduction script is outside it.** A script written to validate a test must carry the same isolation as the test it validates, or it is a more dangerous version of the thing under test. Candidate fix is a guard in `run_update` refusing to run when `HOME` is the real one and `MOCK_CALLS_FILE` is unset, so the destructive path cannot be reached by hand at all. |
| `update_aws_cli` and `update_rust` swallow every failure except `cd`, so the new aws/rust section rc overstates what it knows | `6e19ed0` wrapped both in `_update_record_start/end`, which is a real improvement — a `cd` failure is now visible where nothing was before. But `lib/developer.sh:17-35` checks **nothing else**: `curl` fetching `AWSCLIV2.pkg`, `sudo -H installer`, `rm`, `unzip`, and the three `rustup` invocations per branch are all unchecked, so only `cd "${HOME}/software_downloads/awscli" || return 1` can propagate. A 404 from the AWS CDN, a failed install, or a failed `rustup` still records **`[OK] aws`** / `[OK] rust`. That commit's message says "a real failure on a real machine was reported nowhere" — after the change most real failures still are, and the message oversold it. This is `USER.md`'s trust-signal rule in the repo's own code: an `[OK]` that cannot see a failed install is not an OK. Fix is to check each step in `lib/developer.sh` and propagate, in the shape `shell.md`'s return-code-propagation section prescribes; `_rustup_found` is also assigned twice and never read there. Deliberately scoped out of `6e19ed0` under Surgical Changes rather than widened into it. Found by the Task 6 code-quality reviewer. |
| 18 pre-existing `run run_update` sites assert no `$status` | Task 5 (`8a7eb9f`) required every **converted** site to gain `[ "$status" -eq 0 ]`, because those tests had relied on bats `set -e` to fail them and wrapping in `run` without an assertion turns a real failure into a pass. That rule governed the 24 sites it converted. It does not reach the 21 sites already in the wrapped form beforehand, and 18 of those assert only on `$output` or `MOCK_CALLS_FILE` — a non-zero `run_update` is silently tolerated there. Sites, verified present at `8a7eb9f^`: `workflows.bats` 671, 703, 715, 726, 1005, 1014, 1023, 1762, 1775, 1787, 1806, 2248, 2272, 2290, 2302, 2323, 2347, 2372. So Task 5's product — failures regain their names — is only partial across the file. Natural companion cleanup, not a defect in that commit. Found by the Task 5/6 spec reviewer. |
| `profiles.bats` isolates absent-capability tests per-test, so the trap stays armed for the next one added | `config/profiles.zsh` exports `PROFILE`, seven `HAS_*` and one legacy identity variable into every child of a login shell, and `detect_env` only ever *adds* `HAS_*`. Any test asserting a capability is **absent** therefore reads the login shell's value unless it clears the environment first — green in CI (`ubuntu-latest` never sources `.zprofile`), red on every development machine. Three such tests were fixed in `7480ca5` by adding the clear to each test body, matching the file's existing idiom at `:49-50` and `:384-385`. **The fix is per-test, so nothing protects the next absent-capability test someone writes** — exactly `tdd.md` E2's "a per-test guard leaves the trap armed for the next test someone adds to the file", and nothing mechanical catches the omission; a reviewer has to notice. Candidate fixes: hoist the clear into `setup()` (needs checking first whether any test in the file legitimately depends on inherited values — that check is the work, not the edit), or add a meta-test asserting every `[ -z "${HAS_*` assertion in the file is preceded by a clear in the same body. Raised as an advisory, deliberately not as a finding, by the Task 0 spec-compliance reviewer: hoisting alters every test in the file and exceeded that task's declared scope. **The code-quality reviewer then found the mirror-image defect, which is the larger half: seven *presence* assertions (`[ -n "${HAS_*}" ]` at `:121`, `:128`, `:196`, `:219`, `:237`, `:245`, `:253`) are vacuous on any development machine** — an inherited `HAS_*` survives `detect_env` unchanged, so they pass regardless of what the code does. Those are not merely unprotected like the absent-capability tests were; they currently prove nothing locally, and CI cannot tell you because `ubuntu-latest` has nothing to inherit. That reviewer also checked the `setup()` hoist is **provably safe**: no test in this file consumes an inherited `PROFILE`/`HAS_*`/legacy value as input, `setup()` exports only `PATH`, `MOCK_PYENV_WHICH_STDOUT` and `BATS_VER`, and `legacy_oracle.bash` defines a function only. So the hoist fixes all ten tests at once. Note a third spelling of the same clear already exists at `tests/setup_env/unit.bats:384` (`unset "${!HAS_@}" PROFILE STUDIO`), so a shared helper in `tests/helpers/common.bash` is the wider version of this row. |
| `config/profiles.sh`'s failure under bash 3.2 is positional, not structural | Measured 2026-08-23: `source config/profiles.sh` returns **2** under real `/bin/bash` 3.2, which is what lets `detect_env`'s new propagation fail closed on it. But a sourced file returns its **last executed command**'s status, and this file's last statement is the failing `PROFILE_LEGACY` `declare -A` at `:55`. Two copies differing only by a trailing `printf ''`: `plain rc=2`, `appended rc=0` with `MAP[studio]=[wsl2_workstation]` — still wrong, now reporting success. A guard above the first `declare -A` (gated `-z ${ZSH_VERSION:-}`, since `BASH_VERSINFO` is unset in zsh and a bare version test would take out every login shell via `profiles.zsh`) makes it structural. **Designed and cut** after three review rounds — spec `2026-08-23-profiles-bash-version-guard-design.md`, Out of scope, carries the full reasoning and the six test cases it would need. Cut on the accounting, not on a defect: the class has exactly one observed instance (`21671b8` appending `PROFILE_LEGACY`) and that instance is itself a `declare -A` which *preserves* the rc=2, so **zero adverse instances** against a fourth production file and 6 of 14 test cases, all macOS-only. Revisit if anything ever appends a succeeding statement to that file. **Largely superseded 2026-08-23 by a cheaper fix in the consumer:** Task 1's code-quality reviewer found the same positional-versus-structural weakness one level up — a sourced file returns its *last* command's status, so a failure confined to an earlier statement leaves `source` at 0 with the table incomplete. Measured: `rc=0 LOADED=1 PROFILE=unknown` with `HAS_DEVTOOLS` inherited from the login shell. A `declare -p PROFILE_MAP PROFILE_CAPS PROFILE_LEGACY` post-condition in `detect_env` before it sets the sentinel turns the check into *the three arrays exist*, verified to fail on an incomplete fixture and pass on the real table. That is structural, is not macOS-specific, costs no fourth production file and no macOS-only test tier — so it answers most of what the version guard was for. What it does **not** cover is a bash 3.2 caller reaching `config/profiles.sh` by some route other than `detect_env`; there is no such route today, which is why this row stays open rather than closed. |
| `setup_env.sh -t doctor` cannot run over `ssh` to a mac | `setup_env.sh` is `#!/usr/bin/env bash`, and sshd hands a non-interactive command `_PATH_STDPATH` (`/usr/bin:/bin:/usr/sbin:/sbin`), where `/usr/bin/env bash` resolves macOS's `/bin/bash` 3.2 — measured. `setup_env.sh:20`'s bash-5 guard then exits 1 with `On macOS, run first: ./scripts/bootstrap_mac.sh`, before the Homebrew check and before any workflow dispatches. So `ssh studio 'cd dotfiles && ./setup_env.sh -t doctor'` never reaches `run_doctor`, and neither would any launchd or cron health check. The Linux boxes are unaffected: `/usr/bin/env bash` resolves 5.2 there under the same PATH. Pre-existing and unrelated to the identity-table work; surfaced by the risk lens during Step 8 of the spec above, which correctly noted that 'the operator only runs doctor from a terminal' is a statement about habit rather than a property of the code. Fix is a `PATH` prepend or an explicit interpreter at the invocation site, not a change to the guard. |
| `setup_env.sh:61`'s workflow carve-out is a second copy of the argv carve-out at `:9-18` | The bash-guard change (spec `2026-08-23-profiles-bash-version-guard-design.md`) adds `if ! detect_env && [[ -z ${DOCTOR:-} && -z ${CHECK_VERSIONS:-} ]]` at `:61`, naming two read-only workflows. `setup_env.sh:9-18` already computes an overlapping carve-out from raw argv for `_REQUIRES_BREW_PREREQ`, and that one has a **third** member, `--brew-install`. Two hand-maintained references to one policy with nothing forcing them equal — `behavior.md`'s displacement class: each is correct about its own subject and silent about drift between them. Not a defect today; the two lists differ for defensible reasons (`--brew-install` mutates, so it should abort). Fix is either to derive `:61` from `_REQUIRES_BREW_PREREQ` — note it is `unset` at `:38`, so that needs a rename or a second variable — or to add one assertion that the two lists agree where they are meant to. Found by the risk lens during Step 8 of that spec; deliberately not widened into that diff under Surgical Changes. |
| Inlining the Renovate preset is a stopgap — nine hand-synced copies with no drift detection | Landed 2026-08-23 to unblock dotfiles, which had been structurally dead since 2026-05-18. The end state both sessions argue for is **hosting the preset in a public repo** — `dotfiles` is the natural host: public, already carries fleet-wide artifacts, already vendored from by every repo. **But the host choice is only safe paired with a liveness check**, because a single public host recreates a single point of silent failure: rename it or make it private and all nine die identically and silently again. `ai-config` staying private is right — it is the harness and memory corpus, and that is the wrong reason to flip visibility. **Decide: public-host + liveness check, or keep nine inlined copies and build a drift check.** ai-config would rather land the host move than build a drift gate for copies we intend to stop having. |
| The preset fix is necessary and NOT sufficient — the whole fleet is 77 days silent, including the two repos where the preset resolved | Measured 2026-08-23 with the recency check the math session proposed, which is strictly better than the count I first suggested: `terraform_ansible` **count=5, last 2026-06-06, 77 days ago**; `ai-config` **count=1, same day, 77 days**; all seven others **never**. Those two are PRIVATE and demonstrably resolved the preset — `terraform_ansible`'s PR carries the preset's own `groupName` — so the public/private failure explains *where the six PRs went on one day in June* and explains **nothing** about the 77 days since. **A count hides this completely: 5 reads as healthy and is exactly as dead as 0.** Any liveness check must be *most recent artifact and how long ago*, never a count. **Open: why did Renovate stop entirely in June, in repos with valid resolvable config?** The App is available account-wide (operator-confirmed), the schedule is `before 5am on monday`, and 11 Mondays have passed. Not yet distinguished: App run cadence or installation suspension; genuinely zero updates across nine repos for eleven weeks (implausible — `terraform_ansible` alone carries 239 action refs). Ruled out: automerge hiding PRs, since the query is `--state all`. **ANSWERED 2026-08-23 — the cause is `mode=silent`, set Mend-side, above every repo's config.** The operator supplied real Renovate job logs (`brujack/ai-config`, 15:11Z, renovate 44.39.0): *"Repository is running with mode=silent and will not make Issues or PRs by default"*, then `36 flattened updates found`, `Returning 7 branch(es)`, and seven lines of `Branch <name> creation is disabled because mode=silent`. `"mode":"silent"` is in the resolved config of both logs available and in no repo's `renovate.json`. **It explains every observation at once** — the eleven-week silence in all nine repos rather than just the public ones, the zero Dependency Dashboards fleet-wide (`mode=silent` suppresses them; the earlier attribution to `config:recommended` not enabling them was wrong and is corrected in `ci.md`), and why Mend's `onboarded`/`activated` split maps onto nothing causal — under silent mode no repo can transition by producing output. **Fix is a Mend-side setting, not a config change.** Two hypotheses died here and both were mine: onboarding-PR-never-merged (no onboarding PR exists in any repo) and the preset's five-hour `before 5am on monday` window. The schedule one is **not refuted, it is untestable while silent mode holds** — an experiment removing it would produce a null that reads as "schedule exonerated" when it means "nothing is observable". Branch `test/renovate-schedule-hypothesis` was stood down unmerged for exactly that reason. Worth retesting after silent mode is lifted, because the supporting detail is still real: the only PRs the fleet ever produced arrived on **2026-06-06, a Saturday** — outside that window — consistent with a first run ignoring `schedule` and every later run being gated. **The preset inlining remains necessary and correct**: ai-config resolved its inlined block and extracted 36 updates across 10 workflow files, which a repo that cannot resolve its preset never reaches. Two independent causes, and the second was not what either session guessed. **Narrowed 2026-08-23 by ai-config's `make renovate-liveness` (#196), which adds the state my framing lacked: PENDING.** A repo repaired today with no scheduled window since is not inert, and reporting it as dead reads as the fix having failed. Current: `ai-config`/`terraform_ansible` **ACTIVE**; `dotfiles`(6 pending refs), `etch-cli`(40), `math`(122), `state-ledger`(6) **PENDING**; `ai-devops`/`bjc-site`/`etch-config` **UNKNOWN**, nothing owed. **The preset schedules `before 5am on monday`, so Monday is the decisive test** — if the four PENDING repos still show 0 PRs against real pending work after that window, they flip to INERT and the second cause is confirmed. Until then this row's 77-day claim stands only for the two ACTIVE repos, which is where it was always sharpest: they could always read the preset and have been silent since June anyway. |
| Renovate failing produces the same observable as Renovate having nothing to do — and nothing checks which | **This is the property that cost eleven weeks, and inlining does not fix it.** dotfiles' preset threw `config-validation` at `initRepo` on every run since 2026-05-18, abandoning the repo before dependency extraction — so the output was zero PRs and zero dependency-dashboard issues, which is exactly what a repo with no available updates produces. Nobody could tell the difference, and nobody looked, because a correct-looking `renovate.json` reads as evidence the bot ran. Measured differential on the same instrument, 2026-08-23: old config **8 preset errors, 0 extractions**; inlined config **0 errors, 1 extraction**. **The check that would have caught it is one command** — `gh pr list --author app/renovate --state all` plus looking for a Dependency Dashboard issue, run after any Renovate config change. Currently a human ritual in `ci.md`, which is not good enough. Same class as `uv lock --check`: the gate that was missing was for the relationship nobody was examining. |
| `package.json` declares one dependency that nothing uses, and Renovate does not manage it | Found 2026-08-22 while enabling `pep621`, when the ai-config session flagged the file as unmanaged. Measured before acting: `json2yaml` is the sole dependency, and `git grep` across every tracked file except `package.json` finds it **only** in `docs/` — a retrospective row, and three superseded plan files quoting an `npm install json2yaml` line from a `run_developer_or_ansible()` body that no longer contains it. No `lib/`, `scripts/` or `Makefile` invocation; no lockfile tracked. That reasoning argued against adding `npm` to `enabledManagers` — managing a dead dependency is worse than leaving it unmanaged, because it generates PRs nobody can evaluate; same shape as the pylint removal earlier that day. **It was added anyway, later the same day, in #237, and this row said otherwise until 2026-08-23.** Measured: `enabledManagers` is now `["github-actions", "npm", "pep621"]`, so a `json2yaml` bump will raise a PR against a dependency with no consumer. The row's decision is therefore live and now has a deadline attached to it rather than a preference. **Decide: delete `package.json`, or restore the consumer and enable `npm`.** |
| Nothing ever re-asks the resolver whether the locked answer is still the best one | `uv sync --frozen` made the **install** reproducible; it did not make the **resolution** fresh. `uv lock` against an existing lock is itself conservative — it preserves resolutions the constraints no longer require — so the venv's set is partly an artifact of resolution *history* that the lock froze rather than cleared. Two instances from one day, arriving from opposite directions: dotfiles#227 changed the lock with nobody deciding (checkov walked back a year under a title naming asteval), and `shell-gpt` stayed at 1.0.1 for months after the constraint holding it there was irrelevant. **Floors stop silent downgrades; nothing stops silent hold-backs.** Measured cost of the gap on 2026-08-21: a full re-resolve of the floored manifest moves 272 -> 275 with 5 version changes (`bc-detect-secrets` 1.5.41->1.5.50, `bc-python-hcl2` 0.4.2->0.4.3, `checkov` 3.2.414->3.3.13, `openai` 0.28.1->2.54.0, `shell-gpt` 1.0.1->1.5.1) and 3 additions (asteval, ecdsa, jiter) — verified independently by two sessions on separate trees. **The 272 -> 275 re-resolve above has since landed** (`fix/venv-manifest-corrections`, 2026-08-21) — read those figures as the measured cost of the gap, not as pending work. What remains unshipped is the *recurrence*: nothing re-asks the question on any cadence. Proposed practice, not a gate: periodically run `uv lock --upgrade` and review the diff. One command, reviewable output, and a full re-resolve is a decision rather than a check — which is why it should stay manual. Related: a comment recording an accepted risk is not a constraint; the `ACCEPTED RISK` block predicted #227's exact harm in prose and the resolver never read it. |
| Extract `_snapshot_venv` out of `run_update`'s pip section | maintainability-review S2 (does-one-thing), 2026-08-21. The pip section grew 45 -> 75 lines and `run_update` 326 -> 357, now carrying six responsibilities inline; the snapshot + rotation pair (~18 lines) is cohesive and nameable. Not fixed in-branch: review-lens findings do not block, and it trades against S2c (the extraction would have exactly one call site). The retired shell function-length scanner would have flagged this — see that scanner's own backlog row. |
| `run_update`'s EXIT trap clobbers bats' own, so a failing test loses its name | **Mechanism measured 2026-08-20, and it is not the one first proposed.** `trap -p EXIT` inside a bats test body returns `trap -- 'bats_teardown_trap as-exit-trap' EXIT` — **bats installs an EXIT trap and that trap is what emits the TAP line.** `run_update` sets `trap -- 'rm -rf "${_DOTFILES_RUN_TMPDIR}"; unset _DOTFILES_RUN_TMPDIR' EXIT`, which clobbers it, so a test that calls `run_update` directly and then fails produces **no `not ok` line at all** — only `# bats warning: Executed N instead of expected M tests`. Isolated with a 5-case probe: plain failure reports; trap-then-fail vanishes; tmpdir-trap-then-fail vanishes; **`trap - EXIT` before failing also vanishes**, so clearing the trap is NOT a fix (it removes bats' trap too); a passing test with a trap still reports. Detection survives — the run exits non-zero and `make test` goes red — but diagnosis does not: on a 186-test file a red tells you only that one of them broke. Scope: 43 call sites in `tests/setup_env/workflows.bats`, 7 in `tests/setup_env/unit.bats`. Fix options, in order of robustness: have tests call `run run_update` (subshell confines the trap — what the ai-config session did for its own two tests, with a comment saying why); or give `run_update` a cleanup path that **never installs** an EXIT trap, calling cleanup explicitly on each return path instead. **Do not collapse that with "clear the trap before returning" — they are opposite outcomes.** Clearing runs `trap - EXIT`, which removes bats' trap and is as fatal as clobbering it (probe case D); never installing one leaves bats' trap untouched and works. Third option: chaining onto `bats_teardown_trap` works but depends on bats internals and is version-fragile (measured on bats 1.14.0 locally, CI runs 1.10.0). Found by the ai-config session during python-deps step 2. **Census caveat, worth more than the row: the first count of affected sites was 43, taken on a dirty working tree where two of the counter's own in-flight tests still used the direct form — so their own uncommitted edit was counted as pre-existing debt. The base-ref census (`origin/master`: 41 + 7) is the control that caught it, and it was only run because two independent counts disagreed. Take any population census at the base ref, not at HEAD.** |
| brew is unreachable from `ssh workstation '<cmd>'` — CLOSED, do not redesign without a consumer | **Four designs were built and all four blocked, 2026-08-20. Read the specs before reopening.** `ssh host '<cmd>'` runs a non-login non-interactive zsh reading only `.zshenv`, so `brew` and every brew binary are unreachable. **The operator has confirmed `setup_env.sh` is only ever run by a human at a prompt** — no cron, no timer, no hook, no CI job invokes it, measured. The working answer is the 34-character `PATH=/home/linuxbrew/.linuxbrew/bin:$PATH` prefix that `CLAUDE.md` already documents. The designs died on: (1) `.zprofile` targets an actor with no producer; (2) `.zshenv` prepend swaps `python3` 3.12.3->3.14.7 and breaks `import apt_pkg`; (2b) the exported append degrades every tmux pane via the oh-my-zsh presence guard; (3) `setup_env.sh` probe has no consumer, and its immunity claim was false. **The governing lesson: any change making `brew` resolvable earlier flips every presence-guarded conditional downstream of it** — `3_oh_my_zsh.zsh`'s plugin, `.zprofile:5`'s pyenv, `developer.sh:178` rbenv, `developer.sh:302` pyenv, `workflows.sh:49` claude. Nobody has enumerated the full set. Reopen only when a named automated consumer exists; `specs/2026-08-20-setup-env-brew-probe-design.md` carries the salvage path and the four suite repairs it would need. |
| `.zprofile:12`'s rbenv init duplicates its shim path in nested login shells | Measured 2026-08-20 on the workstation: `env -i HOME=$HOME zsh -lc` nested three deep yields 2 copies of `~/.rbenv/shims` on PATH. `eval "$(rbenv init - --no-rehash zsh)"` prepends unconditionally, and `typeset -U path` — which would absorb it — lives in `6_path.zsh`, an interactive-only file, so a login-only shell never dedups. Harmless today (duplicate entries resolve identically) but it is the same non-idempotent shape that sank the linuxbrew login-PATH design, and `code-standards.md` calls idempotency a cornerstone. Nesting is the normal workflow, not a contrived depth: `tmux default-command` is empty so every pane is a login shell, and ssh -> tmux -> pane is three levels. Fix is a membership guard or moving `typeset -U path` somewhere every actor reads. |
| `grep -q` in `tests/setup_env/linux_ubuntu.bats` lets a real deletion pass | `:127` asserts `grep -q "brew install pyenv"`, which also matches the `brew install pyenv-virtualenv` line, so the test cannot fail for the formula it names. Verified twice 2026-08-20 — by the T2 code-quality review and independently in a scratch `git archive` copy: delete `brew_install_formula pyenv` outright and `bats -f "installs pyenv via brew"` still reports `ok 1`. `uv`'s new case at `:139` uses the anchored `grep -qx` for exactly this reason and kills the `uv`->`uvx` mutant that `-q` survives. Audit and convert the other prefix-collision candidates flagged in the same review (`bat` vs `bats-core`, `helm` vs `helmfile`), and check the same idiom in sibling bats files rather than only this one — the collision is a property of the assertion form, not of these three formulae. |
| `[ "$status" -eq 0 ]` on `_install_ubuntu_brew_packages` is a near-tautology | The function's `elif` branch ends `brew trust ... 2>/dev/null || true` (`lib/linux_ubuntu.sh:382`), so it returns 0 even when every `brew install` inside it fails — measured 2026-08-20 with `MOCK_BREW_INSTALL_EXIT=1`: status 0, test still passes. All 12 cases in that file carry the assertion, so it is idiom-consistent rather than a regression, and the `grep` that follows is what actually discriminates. Worth deciding deliberately: either propagate failure out of the function (which changes install behaviour on a partially-broken machine and wants its own cycle) or drop the status assertion as decoration. Do not fix one call site in isolation. |
| `brew-drift` measures Linux against a macOS Brewfile it never installs from | `_update_check_brewfile_drift` (`lib/update_summary.sh:644`) guards only on `quiet_which brew` and the Brewfile existing — no platform check — while Linux installs from `_install_ubuntu_brew_packages()` (`lib/linux_ubuntu.sh`), a different list. Measured 2026-08-20: the workstation has **77 of 127** active-Brewfile formulae missing (`automake`, `bash`, `git`, `go`, `gnupg`, `coreutils`, …), against **3 of 128** on the Studio. So the drift line is permanently WARN on Linux and carries no signal there — an operator learns to ignore the one check that would tell them a real formula went missing. Two candidate fixes: platform-guard the check, or give Linux its own expected-set derived from `_install_ubuntu_brew_packages()`. The second is the one the ai-config Python-dependency design will want, since it makes the Linux install list a first-class tracked set rather than an inline function body. Found while verifying `uv`'s Task 3 acceptance was reachable. |
| `CLAUDE.md`'s `load_setup_env()` claim is false | Testing Rules says it sources `setup_env.sh` -> `detect_env.sh`, "setting `MACOS=1` on macOS or `LINUX=1` + `UBUNTU=1`". Measured 2026-08-20: the sourcing guard returns before `setup_env.sh:61`'s `detect_env` call, so `MACOS`, `PROFILE` and every `HAS_*` are unset after `load_setup_env()` — verified by `env -u MACOS -u HAS_* bash -c 'source setup_env.sh; ...'` -> `MACOS=[] PROFILE=[] HAS_count=0`. OS-branching tests get those vars from the parent shell's exported environment instead, which is why they pass. Found by the T1 code-quality review during the `uv` Brewfile cycle; out of scope there. Fix needs a sweep of which tests actually depend on the inherited vars before the doc is corrected, since the honest correction may also be a test-isolation finding. |
| `config/profiles.sh` silently mis-resolves under bash 3.2 — no version guard | The file header says "requires bash 5+" and nothing enforces it. Under `/bin/bash` 3.2 (macOS system bash) `declare -A` errors, the array literal collapses to an indexed array, and a string subscript resolves to **element 0 or the last element** rather than failing. Measured 2026-08-18 on the Studio, hostname `studio`: master `PROFILE_MAP[studio]` -> `wsl2_workstation` (want `mac_workstation`), and post-#223 `PROFILE_LEGACY[studio]` -> `CRUNCHER` (want `STUDIO`), so `detect_env` would set `readonly CRUNCHER=1` — and `readonly` inside a bash function is global and irreparable for the process. **Not fail-closed**: a security-review pass described the new table as returning empty under 3.2; it returns a wrong value, same as the old one. Reachable because `setup_env.sh` is `#!/usr/bin/env bash` and `ssh host '<cmd>'`, cron and launchd all get `_PATH_STDPATH`, where `/usr/bin/env bash` finds 3.2 — see `CLAUDE.md`'s actor table. Pre-existing on master and out of scope for #223 under Surgical Changes. Fix is a guard at the top of `config/profiles.sh`: `[[ ${BASH_VERSINFO[0]:-0} -ge 4 ]]` or exit non-zero with the remedy named, so a 3.2 caller fails loudly instead of resolving a different machine's identity. Note the zsh reader is unaffected. |
| `.zprofile` duplicates the rbenv init that `5_general.zsh` now gates by capability | `.zprofile:10-14` runs the same `rbenv init` behind `[[ -n ${WORKSTATION} ]] || [[ -n ${CRUNCHER} ]]` with a hardcoded `/home/linuxbrew/.linuxbrew/bin/rbenv` path, no `_OVERRIDE_*` seam, and no bats test sourcing it for rbenv. After dotfiles#223 the interactive path (`5_general.zsh:77`) asks `HAS_DEVTOOLS` while the login path still asks by hostname, so login and interactive answer the same question two different ways. They agree today — `workstation`/`cruncher` are exactly the two hosts carrying `devtools` — but the next Linux devtools host added to `PROFILE_MAP` gets rbenv interactively and not at login, silently. Found by the T1 code-quality review; out of scope there because `.zprofile` was not in `files_touched`. Fix wants the same seam treatment `5_general.zsh` already has, so a test can drive it. |
| `.zshrc:8,11` is the real owner of gcloud sourcing, duplicated in `5_general.zsh` | `.zshrc` sources `path.zsh.inc` and `completion.zsh.inc` from `/opt/homebrew/share/google-cloud-sdk` with **no hostname guard**, five lines below the `.zshrc.d` loop. `5_general.zsh:129-143` sources the same pair behind a `RATNA`/`LAPTOP`/`STUDIO` guard using the pre-2026-04-30 `Caskroom/google-cloud-sdk` cask token. Measured 2026-08-17 with `hostname` mocked to `reception`: completion ABSENT after the `.zshrc.d` loop, LOADED after `.zshrc:11` — so on ARM the duplicate's guard buys nothing and the earlier "completion absent on three work macs" framing is refuted. `.zshrc` hardcodes `/opt/homebrew`, so it no-ops on Intel and `ratna` is served only by the `5_general` copy. Coherent fix: one owner, prefix-aware, no hostname — but `.zshrc` runs AFTER the `.zshrc.d` loop so `_homebrew_prefix_arm`/`_intel` are already unset (`5_general.zsh:270`), it has no `_OVERRIDE_*` seam, and no bats test sources it. Cut from the 2026-08-17 consolidation in review round 3; supersedes the old gcloud row. |
| 26 hand-typed eight-name isolation lines across 4 bats files | `tests/zshrc.d/unit.bats` (17), `tests/zshrc.d/profiles.bats` (4), `tests/zshrc.d/cross_shell.bats` (4), `tests/setup_env/profiles.bats` (2) each hand-type `unset LAPTOP STUDIO RECEPTION OFFICE HOMES WORKSTATION CRUNCHER RATNA` (isolation) or `for v in LAPTOP ...` (reporting). A ninth legacy variable leaves every one stale and the affected assertions pass on the ambient value — `grep -qx "LEGACY=..."` is a membership check (`profiles.bats:173`, `setup_env/profiles.bats:343`). Cut from the 2026-08-17 legacy-identity consolidation on expected-value grounds: converting 1 of 22 was 4.5% of the claimed property, and a bash-side derivation interpolated into a `zsh -c` string fails silent-empty — `unset ${empty}` writes to a stderr the harness does not capture and does not change the `zsh -c` status, so an empty derivation is GREEN in CI and trips only on a dev box with an ambient `STUDIO=1`. Convert all 26 together via one helper sourced by all four files, with a non-emptiness AND count-pinned guard against the real `PROFILE_LEGACY` in front of the derivation — the guard is the load-bearing part (`profiles.bats:121` documents the same class, incl. why a count comparison there was circular). |
| three legacy-identity read sites are really "is this host mapped" | `7_final.zsh:60` tests all eight legacy variables, which is `PROFILE != unknown` spelled out longhand; `.zprofile:6` is the five-mac list and `:10` is the Linux pair. All three are capability or is-mapped questions, not per-host ones. Left out of the 2026-08-17 legacy-identity consolidation, which scoped itself to `5_general.zsh` plus the mapping tables — `.zprofile` runs before `1_init.zsh` and is the one file where a wrong identity test costs the whole login shell, so it wants its own cycle. `2_functions.zsh`'s two sites are covered by the separate `Make()` gmake row. |
| npm global pin bump path                                                                | The three npm pins added in PR dotfiles#192 (`JSCPD_VER`, `FIRECRAWL_CLI_VER`, `EXA_MCP_SERVER_VER`) are bumped by hand — `./setup_env.sh -t check-versions` cannot cover them. Measured 2026-07-29: jscpd works (`releases/latest` 5.0.14 matches npm exactly); firecrawl-cli publishes no GitHub releases at all; exa-mcp-server is actively unsafe to wire in — its `--version` starts the MCP server rather than printing a version, so the checker would hang. A fix needs a per-tool version source (npm registry query) rather than the existing GitHub-releases path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `includeIf "gitdir:"` hooksPath pins stay probe-cwd-scoped                              | Follow-up to the include-borne pin fix (dotfiles#198), which made `_git_hooks_hookspath_offenders` read `--includes`. Unconditional `[include]` pins are now caught at any cwd; a conditional `includeIf "gitdir:..."` pin is caught only when the probe's cwd already satisfies the condition, because the probe runs once per sweep rather than once per discovered repo (deliberate — the pre-loop position is what lets each repo's rc=2 be attributed to the pin instead of counted as its own gap). Strictly more than `--no-includes` ever saw, so not a regression. Closing it means giving the probe repo context (`git -C <dir> config ...` per discovered repo), which restructures the sweep. Not currently reachable on this fleet: no tracked `.gitconfig_*` sets `hooksPath`, and the live boxes report unset at both scopes — measured 2026-08-05. Re-price if a machine-local include ever carries one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| pre-push suite runtime is the real lever, not the predicate                             | Measured during dotfiles#195: `make test` is **9m34s** (574s, uncontended, 1128 tests) and the fail-closed trigger raises the trigger rate from 35% to 47% over the last 400 commits — ~47 additional triggering commits, of which ~26 touch files `make test` demonstrably cannot read (auto-generated digest state, `.warp/settings.toml`, `manifests/*.yaml`, `renovate.json`). **Do not widen the inert set to reclaim this** — that is the allowlist creep ADR-0017 rejects, and the direction is deliberate (a false skip costs a red CI plus a round trip; a false trigger costs local time). The durable fix is making the suite faster: parallelism, or splitting a fast subset from the full run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `core.quotePath` defeats the pre-push path anchors                                      | `git diff --name-only` C-quotes paths containing non-ASCII bytes, emitting e.g. `"scripts/caf\303\251.sh"` with a leading double-quote, which no `^`-anchored alternative matches. Under the fail-closed trigger the direction is now safe (an unmatched path is non-inert, so it _triggers_) — but the inert set's own anchors are still evaluated against the quoted form, so a non-ASCII `.md` would trigger unnecessarily rather than skip. Pre-existing since before dotfiles#195 and currently unreachable: 0 of 392 tracked paths contain non-ASCII bytes. One-line hardening if ever wanted: `git -c core.quotePath=false diff --name-only`. Raised independently by `security-review` and a code-quality reviewer during dotfiles#195, both correctly declining to treat it as blocking.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Maintainability pass (re-scope needed — original premise falsified)                     | Original note said "once the ai-config maintainability gate ships, run a worldclass pass in this repo against its thresholds." The gate **has** shipped, and it ran on dotfiles#195 — returning **SKIP**, because its blocking surface is Python `ruff` C901/PLR09xx and Rust `clippy`, and this repo is bash. There are no mechanical thresholds here to run a pass _against_, so the row cannot be executed as written. Two real options: (a) close this row and instead act on the concrete bash findings the gate's report-only lens did produce — chiefly the `install_git_hooks_all_repos` length row below; or (b) spec a bash threshold set for `maintainability-review` in ai-config first, which makes this row a dependency on that work rather than on the gate's existence. Not a free-standing task either way.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Deep-dive bug hunt (fleet-critical)                                                     | **Suitable for an overnight cataloguing run.** This repo deploys and controls every dev machine, so a silent defect here is a fleet-wide defect. Two leads are already reproduced (not hypotheses): `run_update`'s exit code is decorative across all 17 sections, and `GIT_DIR` is stripped in exactly one lib file while `git_sync.sh` and `legacy_rsync.sh` do `git push` over SSH and `rsync --delete` without it. **Full running dossier — leads, defect classes, and the techniques that found them — lives in [`ai-config/docs/knowledge/dotfiles-bug-hunt-leads.md`](https://github.com/brujack/ai-config/blob/master/docs/knowledge/dotfiles-bug-hunt-leads.md). Append there as findings accumulate; do not duplicate them into this row.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `install_git_hooks_all_repos` is ~4x the shell function-length threshold                | 198 lines against `maintainability-review`'s 50-line shell rule (report-only, no ratchet). Grew 142→198 in dotfiles#194; `run_setup_user` grew 70→96 in the same PR. Both were already over threshold at base, so neither is a regression introduced there, but the sweep function is now the largest in `lib/`. Extraction candidate — the discovery loop, the per-repo classification, and the summary assembly are three separable concerns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CI's pinned third-party download has no retry, so one 503 fails the whole job           | Measured 2026-08-12 on dotfiles#207: `curl: (22) The requested URL returned error: 503` from GitHub's release CDN failed the `test` job in **12 seconds**, before a single test ran, on a docs-only six-line comment change. `gh run rerun --failed` cleared it. The sharp form of the finding is not "add a retry": `ci.md`'s rule makes the download **correct** — pinned version, `sha256sum -c`, `-f` so an HTTP error is a curl failure — and every property that makes it correct also makes it a hard, un-retried dependency on a CDN being reachable at that instant. The _unsafe_ version (`apt-get install shellcheck`, mirrors, caching) is more available. So the standard traded availability for integrity without noticing it made the trade. `curl --retry 3 --retry-delay 2 --retry-all-errors` buys the availability back at **no cost to the integrity** — `sha256sum -c` still gates execution either way — which is why this must not be read as weakening the checksum rule. Caveat: `--retry-all-errors` needs curl 7.71+ and is a no-op-with-warning below that, so pin the claim to the runner image rather than to curl generally (`ubuntu-latest` is well past it). Belongs in `ci.md` once and fanned out, not fixed per-repo: **ai-config, math, terraform_ansible and etch-cli all install shellcheck this way**, so every repo following the mandated pattern shares the single point of failure.                                                                                                                                                                                                                                                                           |
| `_ledger_write_run_entry` is 131 lines, past the Google guide's 100-line rewrite signal | Was already over at 103 lines before dotfiles#214, which grew it to 131 (+27%) by adding the `make_version` fragment. Nothing mechanical blocks it — `shell.md` records that the function-length scanner was deleted under ADR-0056 and `jscpd` was never installed, so `maintainability-review` on a shell-only diff examines nothing. Extraction candidate: the base-JSON assembly, the run-type-specific block, and the OS/version probing are three separable concerns. Deliberately not fixed in #214 — refactoring an already-oversized function inside a PR about something else is the scope creep `behavior.md` names.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `tests/mocks/dpkg-query` legacy form was fragile by accident                            | The `-f`-consuming parse on the silent-false-success branch fixed the defect; the reason the intermediate heuristic (`028f375`: `_last="${*: -1}"`, then `${!_var}` indirect expansion) _appeared_ to work is the transferable half, and the two interpreters diverge. **Measured 2026-08-14 against the recovered file, both directions.** Under bash 5.2.21 (CI) and 5.3.15 (Studio), `package_capture.sh`'s two-token `-f '${Package}\t${Version}\n'` makes `_var` an invalid identifier; bash prints `invalid variable name` to **stderr** — not `bad substitution`, which is what an earlier version of this row said and what someone would fruitlessly grep for — aborts the enclosing `if` (skipping both its `printf` and its `exit 0`), and resumes after `fi` at the `case`, which emits the correct legacy output. So **stdout was right and exit was 0 while the mock was broken**; only stderr carried the signal. Under `/bin/bash` 3.2.57 there is **no error at all** — empty stdout, exit 0, clean stderr — a silent wrong answer rather than the same mechanism, which is the opposite of what this row previously claimed. Nothing invokes the mock under 3.2 (the shebang is `#!/usr/bin/env bash`, resolving to Homebrew 5.x), so 3.2 is unreached rather than safe. **The reusable rule: assert stdout and exit code together, because stdout alone passes against a defective mock.** Worth a scan for other mocks relying on the same accidental recovery. Class write-up (this instance plus two others, arrived at independently the same day) lands in ai-config as `ai-config-assertion-satisfied-by-a-second-producer` — cite that rather than restating the mechanism here. |
| `doctor` cannot see a missing gate dependency                                           | `run_doctor` checks tool presence for `envsubst` and `claude` but not `jscpd`, which is a **gate dependency**: without it `maintainability-review`'s duplication check fail-opens, so Phase 3 returns PASS and nothing distinguishes "duplication checked" from "duplication not examined". Measured 2026-08-14 — jscpd was absent on the Linux workstation for an unknown number of PRs and nothing reported it; found only because a peer session went looking. Two causes, both closed for now but neither structurally: jscpd installs in `run_developer_or_ansible`, which `-t setup` never reaches (`setup_env.sh:83-85`), and on that box `setup_env.sh` could not run non-interactively at all (see the Linux `PATH` gate in `CLAUDE.md`). **Operator declined the code change on 2026-08-14, preferring to run `-t developer`** — recorded because relying on remembering to run a workflow is what failed here, and the Windows box inherits the same exposure the day it becomes the third dev machine under a both-boxes-down failure. A check is ~6 lines following the existing `command -v` pattern plus a test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Dead coverage-badge crontab entry                                                       | The nightly job invokes a make target this branch deleted. 78 runs, zero successes, blocked by the bats guard under a cron PATH lacking `/opt/homebrew/bin`. CI has published the badge on every PR throughout — 5 of 6 commits on `coverage-data` are `github-actions[bot]`, the sixth being the orphan-branch creation. Operator action on a machine-local crontab; no repo change reaches it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `wsl2_workstation` is wrongly declared snap-less                                        | `config/profiles.sh`, declared before WSL2 supported systemd. This branch's snap gate no longer depends on it (it probes `command -v snap`), but six `HAS_SNAP` sites in `lib/linux_ubuntu.sh` still do. Unmeasurable remotely — that host runs Windows/WSL2 and accepts no inbound SSH.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `CHRUBY_LOC`'s trailing slash differs between the ARM and Intel branches                | `5_general.zsh` emits `${prefix}/opt/chruby/share/` on ARM and `${prefix}/opt/chruby/share` on Intel — inherited from the pre-2026-08-16 literals and preserved deliberately during the seam unification, under Surgical Changes. Verified safe at the time: `CHRUBY_LOC` has exactly three consumers, and none compares the string — `5_general.zsh:70-72` interpolates it (`${CHRUBY_LOC}/chruby/…`, where a doubled slash is inert), `lib/helpers.sh:320`'s `run_doctor` only prints it, and `lib/detect_env.sh:65-67` is a separate bash-side assignment with its own literals. Normalising is a behaviour-identical one-line diff; the reason it is a row rather than a fix is that it was found inside an unrelated change and nothing depends on it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| two mocks crash instead of degrading when `MOCK_CALLS_FILE` is unset                    | `tests/mocks/xargs` and `tests/mocks/gpg` write to `"${MOCK_CALLS_FILE}"` with no `:-` fallback; the other 59 mocks that touch it all have one (measured 2026-08-16: 2 of 61). Any test that reaches a real `xargs` without setting the variable crashes rather than degrading, and the crash surfaces far from its cause — it cost T3 of the zsh-identity plan a debugging detour, arriving via `.zprofile`'s own `/opt/homebrew/bin` prepend shadowing the mock `pyenv` with the real one, whose `rehash` then invoked the real `xargs`. Two-character fix each; the value is in the sweep, since a mock that crashes is indistinguishable from a production bug at the point it fires.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `Make()` hardcodes gmake paths by hostname                                              | `2_functions.zsh:8,12` route to `/usr/local/bin/gmake` (RATNA) or `/opt/homebrew/bin/gmake` (five-mac list), duplicating what `6_path.zsh`'s gnubin prepend already does. Same disease as the zsh-identity work and deliberately left out of it: removing the hardcode means relying on `PATH`, and `CLAUDE.md` records a live interaction where a `PATH` prepend inside a hook shadows the test suite's own `make` mock (28 of 36 tests failed). Needs its own cycle with the mock interaction designed for, not a one-line swap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| The update summary discards every recorded result                                          | `lib/workflows.sh:106` sets `_DOTFILES_RUN_TMPDIR=$(mktemp -d)` with an EXIT trap, so every `status_*`/`result_*`/`err_*` file `_update_record_end` writes dies with the process — `-t update` has no durable record of what it did on any machine. Compounded on the pip arm, where `printf "Updated pip packages"` runs unconditionally *before* `_update_record_end "pip" "${_pip_rc}"`, so a failing pip step prints success to the operator. Related: `lib/package_capture.sh` has zero production callers and hardcodes previous state to `"[]"` at `:130` (brew), `:136` (apt) and `:142` (pip), so even wired up it reports every package as added and can never emit a diff — the observability gap is all three ecosystems, not just pip. Found by the ansible session while measuring the venv. |
| `uv` ships unpinned and no check in this repo can see it drift                                | Deliberate, recorded in `specs/2026-08-20-brewfile-uv-design.md`: `uv` lands via `brew "uv"` with no version pin, and `-t update` runs `brew upgrade --yes`, so the two development machines track whatever brew ships on the day each runs. **Nothing here can observe that drift.** `brew-drift` compares formula *names* through `comm` over sorted lists, so a version delta is structurally invisible to it; `-t check-versions` reads pinned constants in `lib/constants.sh`, and this decision deliberately adds no `UV_VER`. So the revisit depends entirely on someone recalling one sentence in a spec, and the discovery event would be a lockfile disagreement — the worst place to learn it. **Revisit before step 3 of the parent design, not step 4 as first written**: step 3 rehearses `uv sync` against the real `~/.pyenv/versions/ansible`, `uv sync` prunes by default, and the rehearsal's *output* is what decides step 4 — two machines rehearsing under different `uv` versions produce a comparison that was never valid. Cheapest adequate fix at that point: pin both machines to one `uv` for the rehearsal and record the version beside each result. Full fix if it recurs: `UV_VER` in `lib/constants.sh` plus a checksum-verified GitHub-release install, the pattern `gitleaks` and `cf-terraforming` already use. |
| The coverage tracer matches `BASH_SOURCE` verbatim, so a `../`-invoked script may be traced and discarded | Reported by the ai-devops session, who measured **0% with 3764 trace lines captured and discarded** in their own tracer built from ours. **Mechanism confirmed here empirically** rather than inherited: `bash-tracer.sh:20` sets `PS4='COVTRACE:${BASH_SOURCE}:${LINENO}: '`, and `BASH_SOURCE` records the path *the caller used* — a probe invoking `"$T/tests/../sub/x.sh"` recorded exactly that, not the canonical form. `run-bash-coverage.sh:726-729` then extracts the path verbatim, so a trace line attributed to `/repo/tests/setup_env/../../setup_env.sh` cannot match the instrumented entry `/repo/setup_env.sh`. **Exposure measured: 4 instrumented files are reached through `${BATS_TEST_DIRNAME}/../../` in the suite** — `setup_env.sh` (21 call sites), `lib/helpers.sh`, `lib/git_hooks.sh`, `lib/developer.sh`. **IMPACT DELIBERATELY UNMEASURED.** dotfiles reports 91%, not 0%, so those files evidently also accumulate coverage via canonical paths and the effect here is partial depression rather than total loss — but *how much* is not established, and claiming a figure without measuring it is the error this corpus keeps paying for. **To measure:** count trace lines whose path contains `/../` against total, per instrumented file. **Fix, if it lands:** lexical normalisation applied to **both** sides — not `realpath`, which is absent on macOS by default and follows symlinks, breaking `/tmp` → `/private/tmp` a second way. Direction of the error is understatement, so nothing was ever over-reported. Pairs with the numeric-case-label row below; both want the same re-baseline commit, since either fix moves a CI-gated figure. |
| The bash coverage tracer's case-label exclusion cannot match a numeric label | Found by the ai-devops session 2026-08-22 while building a tracer from `scripts/run-bash-coverage.sh` as its reference. The heuristic excludes bare `case`-branch labels from the coverable count using a character class of `[a-zA-Z_*]`, which never matches `0)`, `1)`, `2)`. Verified here against real bash: `bash -x` emits `+ case "${v}" in` but **no** trace line for either an alphabetic or a numeric label, so numeric labels are uncoverable by construction and counting them is a permanent shortfall attributed to untested code. **Impact measured on this repo: 2 lines, both in `lib/git_hooks.sh:488` and `:493`, out of 3516 coverable = 0.057%.** Direction matters — it **understates** coverage, so the published 91% is if anything conservative and nothing was ever over-reported. Deliberately not folded into #235: the fix is a one-line character-class widening but its consequence is a move in a CI-gated figure, which is not a one-liner. Fix and re-baseline together. ai-devops also reports two tracer issues that do **not** apply here and are recorded so nobody re-derives them: `SHELL := /bin/bash` handing recipes to macOS bash 3.2.57, which predates `BASH_XTRACEFD` and writes `set -x` to fd 2 (this repo's instrumented files do not run under system bash), and a FIFO reader deadlock when `BASH_ENV` is exported across a run that invokes `make` (this tracer appends to a plain file). |
| The pre-commit hook resolves ggshield but never checks it is *usable*, and an unusable scanner blocks every commit | Raised by the ai-devops session 2026-08-21, after the resolution fix landed. `command -v` — and the absolute-path fallback that replaced it — test **presence**, not **usability**. `scripts/pre-commit-hook.sh` runs `ggshield secret scan pre-commit \|\| exit 1`, so a scanner that resolves and then fails for an environmental reason blocks the commit, and the unblock an author reaches for is `--no-verify`, which drops `make lint` and the whole chain — the all-or-nothing bypass `git-workflow.md` documents as the most expensive move available. **Measured jointly, both sessions, and the exit codes differ by SUBCOMMAND:** `ggshield api-status` returns **0** for ambient, **0** for a bad key and **3** for an unreachable instance; `ggshield secret scan path|pre-commit` returns **0**, **3** and **3**. **So `api-status`'s exit code is unusable as a health check — it exits 0 while printing `Status: unhealthy (Invalid API key.)`, reporting health as stdout data rather than as a status.** Anything shaped `ggshield api-status >/dev/null 2>&1 \|\| warn` reads a revoked credential as healthy. A pre-flight probe must parse the output for `healthy`, never branch on `$?`; the only invocation giving a usable exit code is the scan itself. `GITGUARDIAN_API_KEY` **is** consulted and beats the cached `ggshield auth login` token (it printed `Invalid API key.` under the override), so the earlier cached-token doubt is resolved; what remains untested is a genuinely revoked server-side credential, which could differ from a malformed one. The design question is the operator's, not a mechanical fix: `USER.md` says *fail closed on unknown*, which argues for blocking; the `--no-verify` cost argues that a three-way outcome (found=block, unusable=warn-loudly-and-continue, absent=warn-loudly-and-continue) surfaces more rather than less on the degraded path, which the same document also asks for. ai-devops is adopting three-way in its own hook, which is new and has no installed base to change. |
| `auto-merge` accepts a lockfile-only diff, and the bot that produces them is undeclared | **Two findings, one path.** (a) `.github/workflows/ci.yml`'s `auto-merge` job gates only on `needs: [test, lint-macos, powershell, bash-coverage, secret-scan]` — nothing declines a PR whose diff touches `uv.lock` without touching `pyproject.toml`. That is exactly #227's shape (`uv.lock +20/-85`, manifest untouched), and it merged because an internally consistent lock passes every check. A lock-without-manifest condition is **actor-agnostic**, which is the property it needs. (b) The actor was **Dependabot**, not Renovate — verified: #227 author `app/dependabot`, and `renovate.json` is `enabledManagers: ["github-actions"]`, so Renovate manages no Python here (see the row below, which already said so). Measured 2026-08-21: **0 of 9 personal repos carry a tracked `.github/dependabot.yml`** — Dependabot is enabled in GitHub repo *settings*, so the path that acted is governed by no tracked artifact and is invisible to anyone reading the repo, while two tracked artifacts (`renovate.json`, the shared preset) describe automation that did not act. `USER.md`'s allow-path rule: a default-deny chain with an undeclared allow-path beside it. Options are a tracked `dependabot.yml` per repo (even schedule-only, to make it reviewable), or disabling Dependabot version-updates and letting Renovate own Python via `pep621` so there is one path rather than two. Do NOT build this as a `renovate-triage` rule — that was the originally agreed fix and it targets a bot that was not involved. |
| `legacy_oracle.bash`'s comment is 5:1 against its code, duplicating ADR-0021                 | Measured 2026-08-18 after the fourth revision of that receipt: **81 comment lines defending 16 lines of code** (5.1:1, up from 4.2:1 on master), while `docs/adr/0021-…md` carries 119 lines of the same substance and is linked from it. The defending comment is justified — the duplication it protects is deliberate and `code-standards.md`'s anti-pattern test is met — but the *volume* is not: a reader of that file needs the rule and the two re-verification caveats at the moment of temptation, not the full derivation, the four-revision history, and the 2x2. Those belong in the ADR, where they now are. Deliberately **not** trimmed on the branch that produced this measurement: that branch's job was correcting a false receipt, and every previous error in this comment arrived while doing two things to it at once. Trim to the rule plus the caveats plus the ADR pointer, as its own change — and the trim **must preserve the "legacy variable" qualifier** on the pair-choice criterion. That qualifier has already been lost once in compression (the `CLAUDE.md` restatement said "no other assertion names host-specifically", which two greps refute), so a trim that drops it reproduces, in the file being trimmed, the exact defect the trim is meant to tidy around. |
| a second `detect_env` call warns on stderr and is silently tolerated                        | `lib/detect_env.sh` assigns identity via `readonly`, so calling `detect_env` twice in one bash process emits `lib/detect_env.sh: line 6: MACOS: readonly variable` and `line 54: <LEGACY>: readonly variable` — and the function still returns 0, because the `[[ -n ${legacy} ]] && readonly ...` form is not its last command. Measured 2026-08-18 as **byte-identical on `origin/master`** (same 2 lines, same variables, same line numbers), so pre-existing and not introduced by dotfiles#223. Unreachable today: `setup_env.sh:61` is the only production call site, and nothing pins that — no test asserts single-invocation, so a second call site added later would land silently. Either pin the invariant with a test or make the second call a clean no-op; do not simply switch to `export`, since `readonly` is deliberate on the bash side (see `CLAUDE.md`'s export/readonly asymmetry note). |
| the `no_legacy` exception path asserts nothing                                             | `tests/zshrc.d/profiles.bats`'s per-host loop and `tests/setup_env/profiles.bats`'s bash equivalent both carry a `no_legacy` associative array — an explicit, reviewable set of hosts that intentionally have no legacy identity variable. It is empty today, and the branch that handles it asserts **nothing**: not that such a host has no legacy variable, not that the one it wrongly has is unexported, not that its `PROFILE`/`HAS_*` are otherwise correct. So the first host ever added to that set is silently untested for the exact property the set exists to declare. Found running pr-review against my own fix commit in dotfiles#223, where the new export-ness assertion had inherited the same nesting (fixed in `8d6fdf4` by hoisting it out; that closes export-ness only). Needs a fixture host in `PROFILE_MAP` plus a positive assertion that the no-legacy contract holds, which is more than a moved line. |
| `pip check`'s verdict is discarded and `SKIP_UPGRADE` is inert                             | `lib/workflows.sh:525` runs `"$PYTHON" -m pip check || true` on every `-t update`, so the one command that detects an over-constrained venv has its rc thrown away; its stdout is also outside the `tee` that closes at `:522`, so nothing parses it either. Measured on the Studio 2026-08-18: rc=1 with five live conflicts (`shell-gpt`/`openai`, `bpytop`/`psutil`, `checkov`/`packaging`, `radon`/`mando`, `pylint`/`astroid`), none of them new and none ever reported. Separately `:508`'s `SKIP_UPGRADE = {packaging, pathspec, rich, psutil, wheel}` is inert for its stated purpose — it filters the *named* upgrade set, not what the resolver moves as a transitive dep, and `packaging 26.3` (checkov wants `<24.0`) and `psutil 7.2.2` (bpytop wants `<6.0.0`) are both past the exact bounds it exists to protect. Un-swallowing the rc is one line and a prerequisite for any pinning work rather than part of it; the denylist is the standing evidence against pinning a hand-chosen subset. **And `pip check` alone is not sufficient** — it tests constraint *satisfaction*, not exposure. The ansible session measured the two load-bearing machines in two different stable states from the same script: Studio at `packaging 26.3` / `wheel 0.48.0` with the checkov bound violated and `pip check` rc=1, workstation at `packaging 23.2` / `wheel 0.45.1` with every constraint satisfied, `pip check` clean, and CVE-2026-24049 unpatched. The second is the script's own steady state (the Studio only differs because of a hand upgrade), so the un-swallowed rc fires on the patched machine and stays silent on the vulnerable one — it needs `pip-audit` beside it, not instead of it. The denylist is therefore not a safety measure but an undocumented daily decision to keep checkov over a security fix, and its comment states the constraint without its cost. Related: the `_pip_pkgs` row below. |

## Adding a new entry

When a new spec or plan is created, add a row to the All Plans table. Set status to **In Progress** when implementation starts, **Done** when the PR merges. Also add a `> **Status: DONE**` banner at the top of the plan file once complete. Move backlog items to the All Plans table when their spec is written (remove the backlog row).
